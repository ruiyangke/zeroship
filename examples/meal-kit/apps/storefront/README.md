# Gather storefront

The customer app in the [Gather monorepo](../../README.md). It owns browsing,
box building, checkout, accounts and cooking. Its server calls the back office
through the signed commerce connection; it does not own commerce tables.

Use the example root's setup and development commands to run the connected
pair. This directory owns the storefront's build and deploy configuration.
