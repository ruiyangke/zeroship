# Gather back office

The staff app and commerce owner in the [Gather monorepo](../../README.md).
It owns recipe approval, country menus, inventory, orders, support, team
permissions, database migrations and provider orchestration.

Use the example root's setup and development commands to run the connected
pair. This directory owns the back office's build and deploy configuration.
