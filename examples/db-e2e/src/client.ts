const app = document.querySelector<HTMLDivElement>("#app");

if (app) {
  app.textContent =
    "db-e2e builds a runnable SQLite demo for @zeroship/db. Run the e2e harness to exercise the full surface.";
}
