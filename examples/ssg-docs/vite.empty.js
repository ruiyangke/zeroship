// Placeholder entry — the SSG demo has no JS, but Vite needs at least
// one input. The build emits an empty `_empty.js` chunk that's never
// linked to from any HTML, so the deploy archive carries 1 KB of dead
// weight. (Skipping rollup entirely is also possible — see README.)
export {};
