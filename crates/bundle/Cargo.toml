[package]
name = "appbase-bundle"
version = "0.1.0"
edition = "2021"
description = ".appbundle binary format — read, write, lazy decompression"

[dependencies]
sha2 = { workspace = true }
zstd = { workspace = true }

[lints]
workspace = true
