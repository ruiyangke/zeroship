-- Generated by schema/generate.mjs through the migration compiler.
CREATE TABLE "workflow"."schema_version" ("id" text PRIMARY KEY NOT NULL, "fingerprint" text NOT NULL);

CREATE TABLE "workflow"."apps" ("app_id" text PRIMARY KEY NOT NULL, "revision" bigint NOT NULL, "policy" text NOT NULL, "signal_epoch" bigint NOT NULL, "last_polled_at" bigint NOT NULL DEFAULT 0);

CREATE TABLE "workflow"."deploys" ("app_id" text NOT NULL, "id" text NOT NULL, "hash" text NOT NULL, "manifest" text NOT NULL, "created_at" bigint NOT NULL, "active" bigint NOT NULL, "state" text NOT NULL, CONSTRAINT "deploys_app" FOREIGN KEY ("app_id") REFERENCES "workflow"."apps" ("app_id") ON DELETE RESTRICT, CONSTRAINT "deploys_pkey" PRIMARY KEY (app_id, id));

CREATE UNIQUE INDEX IF NOT EXISTS "deploy_hash_identity" ON "workflow"."deploys" ("app_id", "hash");

CREATE TABLE "workflow"."schedules" ("app_id" text NOT NULL, "id" text NOT NULL, "name" text NOT NULL, "workflow_name" text NOT NULL, "deploy_id" text NOT NULL, "definition" text NOT NULL, "next_at" bigint, "revision" bigint NOT NULL, "anchor_at" bigint NOT NULL, "last_checked_at" bigint NOT NULL, CONSTRAINT "schedule_deploy" FOREIGN KEY ("app_id", "deploy_id") REFERENCES "workflow"."deploys" ("app_id", id) ON DELETE RESTRICT, CONSTRAINT "schedules_app" FOREIGN KEY ("app_id") REFERENCES "workflow"."apps" ("app_id") ON DELETE RESTRICT, CONSTRAINT "schedules_pkey" PRIMARY KEY (app_id, id));

CREATE INDEX IF NOT EXISTS "schedule_deploy_idx" ON "workflow"."schedules" ("app_id", "deploy_id");

CREATE UNIQUE INDEX IF NOT EXISTS "schedule_name" ON "workflow"."schedules" ("app_id", "name");

CREATE INDEX IF NOT EXISTS "schedules_due_idx" ON "workflow"."schedules" ("next_at", "app_id", "id");

CREATE TABLE "workflow"."runs" ("app_id" text NOT NULL, "id" text NOT NULL, "workflow_name" text NOT NULL, "deploy_id" text NOT NULL, "generation" bigint NOT NULL, "state" text NOT NULL, "control" text NOT NULL, "due_at" bigint, "task_id" text, "lease_epoch" bigint NOT NULL, "key" text, "parent_id" text, "parent_generation" bigint, "parent_ordinal" bigint, "cascade" bigint NOT NULL, "depth" bigint NOT NULL, "created_at" bigint NOT NULL, "terminal_at" bigint, "signal_epoch" bigint NOT NULL, "compensation_target" text, "schedule_id" text, "continued_from_id" text, "continued_to_id" text, CONSTRAINT "run_continued_from" FOREIGN KEY ("app_id", "continued_from_id") REFERENCES "workflow"."runs" ("app_id", id) ON DELETE RESTRICT, CONSTRAINT "run_continued_to" FOREIGN KEY ("app_id", "continued_to_id") REFERENCES "workflow"."runs" ("app_id", id) ON DELETE RESTRICT, CONSTRAINT "run_deploy" FOREIGN KEY ("app_id", "deploy_id") REFERENCES "workflow"."deploys" ("app_id", id) ON DELETE RESTRICT, CONSTRAINT "run_parent" FOREIGN KEY ("app_id", "parent_id") REFERENCES "workflow"."runs" ("app_id", id) ON DELETE RESTRICT, CONSTRAINT "run_schedule" FOREIGN KEY ("app_id", "schedule_id") REFERENCES "workflow"."schedules" ("app_id", id) ON DELETE RESTRICT, CONSTRAINT "runs_app" FOREIGN KEY ("app_id") REFERENCES "workflow"."apps" ("app_id") ON DELETE RESTRICT, CONSTRAINT "runs_pkey" PRIMARY KEY (app_id, id));

CREATE INDEX IF NOT EXISTS "run_continued_from_idx" ON "workflow"."runs" ("app_id", "continued_from_id");

CREATE INDEX IF NOT EXISTS "run_continued_to_idx" ON "workflow"."runs" ("app_id", "continued_to_id");

CREATE INDEX IF NOT EXISTS "run_deploy_idx" ON "workflow"."runs" ("app_id", "deploy_id");

CREATE INDEX IF NOT EXISTS "run_parent_idx" ON "workflow"."runs" ("app_id", "parent_id");

CREATE INDEX IF NOT EXISTS "run_schedule_idx" ON "workflow"."runs" ("app_id", "schedule_id");

CREATE UNIQUE INDEX IF NOT EXISTS "live_workflow_key" ON "workflow"."runs" ("app_id", "workflow_name", "key");

CREATE INDEX IF NOT EXISTS "runs_due_idx" ON "workflow"."runs" ("due_at", "app_id", "id");

CREATE INDEX IF NOT EXISTS "runs_parent_idx" ON "workflow"."runs" ("app_id", "parent_id", "parent_generation");

CREATE TABLE "workflow"."generations" ("app_id" text NOT NULL, "run_id" text NOT NULL, "generation" bigint NOT NULL, "deploy_id" text NOT NULL, "input" text NOT NULL, "output" text, "error" text, "state" text NOT NULL, "started_at" bigint NOT NULL, "terminal_at" bigint, CONSTRAINT "generation_deploy" FOREIGN KEY ("app_id", "deploy_id") REFERENCES "workflow"."deploys" ("app_id", id) ON DELETE RESTRICT, CONSTRAINT "generations_run" FOREIGN KEY ("app_id", "run_id") REFERENCES "workflow"."runs" ("app_id", id) ON DELETE RESTRICT, CONSTRAINT "generations_pkey" PRIMARY KEY (app_id, run_id, generation));

CREATE INDEX IF NOT EXISTS "generation_deploy_idx" ON "workflow"."generations" ("app_id", "deploy_id");

CREATE TABLE "workflow"."steps" ("app_id" text NOT NULL, "run_id" text NOT NULL, "generation" bigint NOT NULL, "ordinal" bigint NOT NULL, "name" text NOT NULL, "occurrence" bigint NOT NULL, "origin_generation" bigint NOT NULL, "kind" text NOT NULL, "state" text NOT NULL, "record" text NOT NULL, "compensation_attempts" bigint NOT NULL DEFAULT 0, "compensation_due_at" bigint, "compensation_error" text, "compensation_retry_ms" bigint NOT NULL DEFAULT 1000, CONSTRAINT "steps_generation" FOREIGN KEY ("app_id", "run_id", "generation") REFERENCES "workflow"."generations" ("app_id", "run_id", "generation") ON DELETE RESTRICT, CONSTRAINT "steps_pkey" PRIMARY KEY (app_id, run_id, generation, ordinal));

CREATE UNIQUE INDEX IF NOT EXISTS "step_name_occurrence" ON "workflow"."steps" ("app_id", "run_id", "generation", "name", "occurrence");

CREATE TABLE "workflow"."tasks" ("app_id" text NOT NULL, "run_id" text NOT NULL, "generation" bigint NOT NULL, "id" text PRIMARY KEY NOT NULL, "worker" text NOT NULL, "epoch" bigint NOT NULL, "token_hash" text NOT NULL, "deadline" bigint NOT NULL, "state" text NOT NULL, "completion_digest" text, "receipt" text, "created_at" bigint NOT NULL, "finished_at" bigint, CONSTRAINT "tasks_generation" FOREIGN KEY ("app_id", "run_id", "generation") REFERENCES "workflow"."generations" ("app_id", "run_id", "generation") ON DELETE RESTRICT);

CREATE INDEX IF NOT EXISTS "tasks_generation_idx" ON "workflow"."tasks" ("app_id", "run_id", "generation");

CREATE UNIQUE INDEX IF NOT EXISTS "task_scope_identity" ON "workflow"."tasks" ("app_id", "run_id", "generation", "id");

CREATE UNIQUE INDEX IF NOT EXISTS "task_fence_identity" ON "workflow"."tasks" ("app_id", "run_id", "generation", "epoch");

CREATE INDEX IF NOT EXISTS "tasks_admission_idx" ON "workflow"."tasks" ("app_id", "state", "deadline");

CREATE TABLE "workflow"."waits" ("app_id" text NOT NULL, "run_id" text NOT NULL, "generation" bigint NOT NULL, "ordinal" bigint NOT NULL, "kind" text NOT NULL, "signal_type" text, "topic" text, "max_signal_age" bigint, "due_at" bigint, "child_id" text, CONSTRAINT "wait_child" FOREIGN KEY ("app_id", "child_id") REFERENCES "workflow"."runs" ("app_id", id) ON DELETE RESTRICT, CONSTRAINT "wait_step" FOREIGN KEY ("app_id", "run_id", "generation", "ordinal") REFERENCES "workflow"."steps" ("app_id", "run_id", "generation", "ordinal") ON DELETE RESTRICT, CONSTRAINT "waits_pkey" PRIMARY KEY (app_id, run_id, generation, ordinal));

CREATE INDEX IF NOT EXISTS "wait_child_idx" ON "workflow"."waits" ("app_id", "child_id");

CREATE TABLE "workflow"."broadcasts" ("app_id" text NOT NULL, "id" text NOT NULL, "topic" text NOT NULL, "signal_type" text NOT NULL, "payload" text NOT NULL, "created_at" bigint NOT NULL, "cursor" text, "finished" bigint NOT NULL, CONSTRAINT "broadcasts_app" FOREIGN KEY ("app_id") REFERENCES "workflow"."apps" ("app_id") ON DELETE RESTRICT, CONSTRAINT "broadcasts_pkey" PRIMARY KEY (app_id, id));

CREATE TABLE "workflow"."signals" ("app_id" text NOT NULL, "run_id" text NOT NULL, "id" text NOT NULL, "signal_type" text NOT NULL, "payload" text NOT NULL, "created_at" bigint NOT NULL, "consumed_generation" bigint, "consumed_ordinal" bigint, "broadcast_id" text, CONSTRAINT "signal_broadcast" FOREIGN KEY ("app_id", "broadcast_id") REFERENCES "workflow"."broadcasts" ("app_id", id) ON DELETE RESTRICT, CONSTRAINT "signal_consumption" FOREIGN KEY ("app_id", "run_id", "consumed_generation", "consumed_ordinal") REFERENCES "workflow"."steps" ("app_id", "run_id", "generation", "ordinal") ON DELETE RESTRICT, CONSTRAINT "signals_run" FOREIGN KEY ("app_id", "run_id") REFERENCES "workflow"."runs" ("app_id", id) ON DELETE RESTRICT, CONSTRAINT "signals_pkey" PRIMARY KEY (app_id, id));

CREATE INDEX IF NOT EXISTS "signal_broadcast_idx" ON "workflow"."signals" ("app_id", "broadcast_id");

CREATE INDEX IF NOT EXISTS "signal_consumption_idx" ON "workflow"."signals" ("app_id", "run_id", "consumed_generation", "consumed_ordinal");

CREATE INDEX IF NOT EXISTS "signals_run_idx" ON "workflow"."signals" ("app_id", "run_id");

CREATE UNIQUE INDEX IF NOT EXISTS "broadcast_delivery" ON "workflow"."signals" ("app_id", "broadcast_id", "run_id");

CREATE INDEX IF NOT EXISTS "signals_mailbox_idx" ON "workflow"."signals" ("app_id", "run_id", "signal_type", "consumed_generation", "created_at");

CREATE TABLE "workflow"."subscriptions" ("app_id" text NOT NULL, "run_id" text NOT NULL, "generation" bigint NOT NULL, "ordinal" bigint NOT NULL, "id" text NOT NULL, "topic" text NOT NULL, "created_at" bigint NOT NULL, CONSTRAINT "subscription_step" FOREIGN KEY ("app_id", "run_id", "generation", "ordinal") REFERENCES "workflow"."steps" ("app_id", "run_id", "generation", "ordinal") ON DELETE RESTRICT, CONSTRAINT "subscriptions_pkey" PRIMARY KEY (app_id, run_id, generation, ordinal));

CREATE UNIQUE INDEX IF NOT EXISTS "subscription_identity" ON "workflow"."subscriptions" ("app_id", "id");

CREATE INDEX IF NOT EXISTS "subscriptions_topic_idx" ON "workflow"."subscriptions" ("app_id", "topic", "id");

CREATE TABLE "workflow"."requests" ("app_id" text NOT NULL, "id" text NOT NULL, "operation" text NOT NULL, "digest" text NOT NULL, "result" text NOT NULL, "expires_at" bigint NOT NULL, CONSTRAINT "requests_app" FOREIGN KEY ("app_id") REFERENCES "workflow"."apps" ("app_id") ON DELETE RESTRICT, CONSTRAINT "requests_pkey" PRIMARY KEY (app_id, id));

CREATE INDEX IF NOT EXISTS "requests_expiry_idx" ON "workflow"."requests" ("expires_at");

CREATE TABLE "workflow"."occurrences" ("app_id" text NOT NULL, "schedule_id" text NOT NULL, "at" bigint NOT NULL, "run_id" text, CONSTRAINT "occurrence_run" FOREIGN KEY ("app_id", "run_id") REFERENCES "workflow"."runs" ("app_id", id) ON DELETE RESTRICT, CONSTRAINT "occurrence_schedule" FOREIGN KEY ("app_id", "schedule_id") REFERENCES "workflow"."schedules" ("app_id", id) ON DELETE RESTRICT, CONSTRAINT "occurrences_pkey" PRIMARY KEY (app_id, schedule_id, at));

CREATE INDEX IF NOT EXISTS "occurrence_run_idx" ON "workflow"."occurrences" ("app_id", "run_id");

CREATE TABLE "workflow"."payloads" ("app_id" text NOT NULL, "run_id" text NOT NULL, "generation" bigint NOT NULL, "id" text NOT NULL, "task_id" text NOT NULL, "hash" text NOT NULL, "size" bigint NOT NULL, "content_type" text, "state" text NOT NULL, "created_at" bigint NOT NULL, "expires_at" bigint NOT NULL, CONSTRAINT "payload_task" FOREIGN KEY ("app_id", "run_id", "generation", "task_id") REFERENCES "workflow"."tasks" ("app_id", "run_id", "generation", id) ON DELETE RESTRICT, CONSTRAINT "payloads_generation" FOREIGN KEY ("app_id", "run_id", "generation") REFERENCES "workflow"."generations" ("app_id", "run_id", "generation") ON DELETE RESTRICT, CONSTRAINT "payloads_pkey" PRIMARY KEY (app_id, id));

CREATE INDEX IF NOT EXISTS "payload_task_idx" ON "workflow"."payloads" ("app_id", "run_id", "generation", "task_id");

CREATE INDEX IF NOT EXISTS "payloads_generation_idx" ON "workflow"."payloads" ("app_id", "run_id", "generation");

CREATE INDEX IF NOT EXISTS "payloads_expiry_idx" ON "workflow"."payloads" ("state", "expires_at");

CREATE TABLE "workflow"."payload_refs" ("app_id" text NOT NULL, "run_id" text NOT NULL, "generation" bigint NOT NULL, "ordinal" bigint NOT NULL, "payload_id" text NOT NULL, CONSTRAINT "payload_ref" FOREIGN KEY ("app_id", "payload_id") REFERENCES "workflow"."payloads" ("app_id", id) ON DELETE RESTRICT, CONSTRAINT "payload_refs_generation" FOREIGN KEY ("app_id", "run_id", "generation") REFERENCES "workflow"."generations" ("app_id", "run_id", "generation") ON DELETE RESTRICT, CONSTRAINT "payload_refs_pkey" PRIMARY KEY (app_id, run_id, generation, ordinal));

CREATE INDEX IF NOT EXISTS "payload_ref_idx" ON "workflow"."payload_refs" ("app_id", "payload_id");

CREATE TABLE "workflow"."outbox" ("app_id" text NOT NULL, "id" text NOT NULL, "kind" text NOT NULL, "payload" text NOT NULL, "created_at" bigint NOT NULL, "delivered_at" bigint, CONSTRAINT "outbox_app" FOREIGN KEY ("app_id") REFERENCES "workflow"."apps" ("app_id") ON DELETE RESTRICT, CONSTRAINT "outbox_pkey" PRIMARY KEY (app_id, id));

CREATE INDEX IF NOT EXISTS "outbox_delivery_idx" ON "workflow"."outbox" ("delivered_at", "created_at");
INSERT INTO workflow.schema_version (id, fingerprint) VALUES ('workflow', 'c2f3c2afd38d3fbe2eaaa83702df60da1b1645cffdb87675c5cec9fed3111834');
