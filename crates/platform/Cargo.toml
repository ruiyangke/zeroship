[package]
name = "appbase-platform"
version = "0.1.0"
edition = "2024"
description = "Multi-tenant platform layer — control plane, metering, billing, enforcement"

[dependencies]
appbase-core = { path = "../core" }
appbase-control = { path = "../control" }
appbase-metering = { path = "../metering" }
appbase-enforcement = { path = "../enforcement" }
appbase-billing = { path = "../billing" }
appbase-plan = { path = "../plan" }
appbase-server = { path = "../server" }

[lints]
workspace = true
