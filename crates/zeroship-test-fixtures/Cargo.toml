[package]
name = "zeroship-test-fixtures"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "Owned infrastructure for mandatory database tests"

[dependencies]
testcontainers = { version = "0.28", default-features = false, features = ["blocking", "watchdog"] }
url = { workspace = true }

[lints]
workspace = true
