[package]
name = "appbase-isolate"
version = "0.1.0"
edition = "2024"
description = "V8 isolate management for appbase — actor model, pool, CPU metering"

[dependencies]
appbase-core = { path = "../core" }
deno_core = "0.395"
deno_error = "0.7"
tokio = { version = "1", features = ["full"] }
serde_json = "1"
libc = "0.2"

[[bench]]
name = "qps_bench"
harness = false

[lints]
workspace = true
