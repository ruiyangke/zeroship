[package]
name = "appbase-enforcement"
version = "0.1.0"
edition = "2024"
description = "Rate limiting, quota enforcement, concurrency control, and entitlements for appbase"

[dependencies]
appbase-plan = { path = "../plan" }
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true
