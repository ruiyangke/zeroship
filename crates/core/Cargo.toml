[package]
name = "appbase-core"
version = "0.1.0"
edition = "2024"
description = "Core interfaces for the appbase platform — Plugin trait, config types, RPC protocol"

[dependencies]
deno_core = "0.396.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"

[lints]
workspace = true
