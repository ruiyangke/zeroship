[package]
name = "appbase-plan"
version = "0.1.0"
edition = "2024"
description = "Quota plan definitions, policies, and period types for appbase"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
