[package]
name = "appbase"
version = "0.1.0"
edition = "2021"
description = "appbase CLI — build, serve, deploy, inspect"

[[bin]]
name = "appbase"
path = "src/main.rs"

[dependencies]
appbase-bundle = { workspace = true }
appbase-runtime = { workspace = true }
appbase-compiler = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
