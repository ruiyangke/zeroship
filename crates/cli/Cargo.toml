[package]
name = "appbase"
version = "0.1.0"
edition = "2021"
description = "appbase CLI — build, serve, deploy, inspect"

[[bin]]
name = "appbase"
path = "src/main.rs"

[dependencies]
appbase-runtime = { path = "../runtime" }
appbase-compiler = { path = "../compiler" }
serde_json = "1"

[lints]
workspace = true
