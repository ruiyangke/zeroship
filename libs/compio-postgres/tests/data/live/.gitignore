# Generated by tests/tls_live_setup.sh: a private CA, a server certificate and
# its key, and the descriptor tests/tls_live.rs reads. All per-machine, all
# regenerated on every setup run, none of it tracked.
*
!.gitignore
