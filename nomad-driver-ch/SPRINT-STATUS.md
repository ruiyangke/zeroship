# nomad-driver-ch sprint status

Auto-maintained by the 10-minute cron + sprint fixers.

## Completed

- [x] T-0 (scaffold) — `ee4a76c3` (`nomad-driver-ch: scaffold from hashicorp/nomad-driver-virt (T-0)`)
- [x] T-0.5 (flake + main.go) — see commits
  - `de35f49e` — `nomad-driver-ch: flake.nix + flake.lock for dev shell (T-0.5 part 1)`
  - `9f1a7e49` — `nomad-driver-ch: cmd/main.go go-plugin entry point (T-0.5 part 2)`
- [x] T-1 (StartTask cold-boot CH spawn) — `6a8d2fcf` (`nomad-driver-ch/start_task: implement cold-boot CH spawn (T-1)`)
- [x] T-2 (graceful stop ladder + DestroyTask + SignalTask) — `dd017c23` (`nomad-driver-ch/stop_task: graceful-stop ladder (T-2)`)
- [x] T-3 (per-VM /30 tap setup + teardown) — `31e05a3c` (`nomad-driver-ch/start_task,stop_task: per-VM /30 tap setup + teardown (T-3)`)
- [x] T-4 (RecoverTask via ch-remote API socket) — `nomad-driver-ch/recover_task: reattach to running CH via API socket (T-4)`
- [x] T-5 (TaskStats: per-task telemetry via host /proc) — `nomad-driver-ch/task_stats: per-task telemetry via /proc+/sys (T-5)`
- [x] T-6 (Restore path: --restore + ch-remote resume wake) — `nomad-driver-ch/restore_task: --restore + ch-remote resume wake path (T-6)`
- [x] G4 (tap rollback on partial StartTask failure) — closes G4 from `docs/comparison-with-nomad-driver-virt.md`. Between `setupTapForVM` succeeding and `handle.SetDriverState` persisting the task, any error return left the tap orphaned (DestroyTask never fires for a task that was never registered in `p.tasks`). Fix: `tapRollback` boolean + defer in `ch/start_task.go`; armed only on the synthesised-net branch (operator-supplied Net still owns its own tap lifecycle). Tests: 64 → 66 PASS (+2: spawn-failure rollback + happy-path-no-rollback).
- [x] C-1 (CH cold-boot spawn argv: long-argv flags, not `--config`) — closes C-1 from `docs/reviews/sandbox-snapshot-restore-cluster-2026-05-25-T8b-smoke-r2.md`. T-1's `cloud-hypervisor --config <json-path>` design choice was wrong: CH v51.1 has NO `--config` flag and rejects with exit 2 ("error: unexpected argument '--config' found"). Cluster smoke r2 caught it after the v2 driver upload + Nomad plugin-stanza fix landed. Fix: `ch/start_task.go::buildSpawnArgv` mirrors the bash wrapper at `crates/sandbox/scripts/nomad-vm-wrapper.sh:638-647` line-for-line: `--api-socket / --kernel / --cmdline / --disk (one flag, N space-separated path= tokens) / --net tap=…,mac=… / --memory size=…M,shared=on / --cpus boot=… / --console off / --serial file=…`. `config.json` is still materialised under the task dir as a debugging artifact (round-trips with the T-6 restore-path rewriter) but is no longer fed to CH. Restore path (T-6, `--restore source_url=file://…`) unaffected — it never used `--config`. Tests: 66 → 74 PASS (+8: 7 new argv-shape pins inc. the C-1 negative `TestStartTask_DoesNotPassConfigFlag`, +1 BuildSpawnArgv pure-fn pin; existing `TestStartTask_SpawnInvokesCH` flipped from `--config` assertion to the long-argv flag set). NOTE: T-8b-smoke-retry-2 needs driver binary v3 (rebuild + GCS upload + pin bump in `gcp-worker-startup.sh` v2→v3); no controller change required.
- [x] C-2 (rootfs staging + disk pre-flight) — `f521eb21` — closes C-2 from `docs/reviews/sandbox-snapshot-restore-cluster-2026-05-25-T8b-smoke-r3.md`. C-1 unblocked the argv parse; cluster r3 caught the next layer: CH crashed at ~42 ms on `VmBoot(DeviceManager(Disk(Os{code:2, kind:NotFound,…})))` because T-1's StartTask synthesised `disks[0] = {Path: runDir/rootfs.img}` without ever staging the file. Under the bash wrapper the cp at `nomad-vm-wrapper.sh:300-305` (`cp --reflink=auto $ZSBX_ARTIFACT_DIR/rootfs-slim.img $RUNTIME/rootfs.img`) handled this; the Go driver bypassed the wrapper, so nothing materialised the rootfs. Fix: `materializeRootfs` mirrors the wrapper's cp verbatim (idempotent `[ ! -f $DISK ]` guard + O_EXCL atomic create + cleanup-on-partial-copy); `artifactDirFromEnv` reads `ZSBX_ARTIFACT_DIR` from `cfg.Env` (the controller emits this on every job under both raw_exec and ChPlugin modes, `crates/sandbox/src/backend/nomad_ch.rs:2339` + `l.2495`); `preflightDiskPaths` stat-checks every disk before spawn (extends the wrapper's existence guards at `nomad-vm-wrapper.sh:270-277` from workspace+user_home to the full disk list incl. rootfs). Every missing disk now surfaces as `ch: StartTask: disk[N] /path/to/foo.img does not exist (controller must stage before spawn)` in the Nomad task log — turning the cryptic CH NotFound crash into an operator-readable error before CH is invoked. Tests: 74 → 81 PASS (+7: 4 rootfs-stage pins inc. happy-path on-disk-effect assertion, idempotency, env-missing, source-missing; 3 pre-flight pins covering missing path, empty file, pure-fn 3-case table). go vet + race clean. NOTE: T-8b-smoke-retry-3 needs driver binary v4 (rebuild + GCS upload + pin bump in `gcp-worker-startup.sh` v3→v4); no controller change required.
- [x] C-7-LT-3 (restore-branch ch.sock readiness probe + per-alloc CH stderr capture) — `3e91da90` (PR1) + `016c8aa1` (PR2) — closes C-7-LT-3 from `docs/reviews/sandbox-snapshot-restore-cluster-2026-05-25-T8b-smoke-r14.md`. First driver-side bug since C-2; HEAD had been stable at `ec6a1de4` for 21 cron fires. Smoke-r14 wedge: at +55s into a wake, nomad-driver-ch's `startTaskRestoreBranch` emitted `Driver Failure rpc error: code = Unknown desc = ch: startTaskRestoreBranch: ch: api socket not responsive at .../ch/local/ch.sock within 10s`. The flat 10s budget on the `--restore` API-socket readiness probe was too tight; cluster wakes had no diagnostic trail for the CH-internal lifecycle (we couldn't tell `started-but-slow` from `crashed`). Fixes:
  - **PR1 (`3e91da90`): retrying ch.sock readiness probe.** `ch/restore_task.go::waitForCHSocketReady` replaces the prior shell-out-to-ch-remote-ping with a `net.DialTimeout("unix", sock, 200ms)` loop, cadence 100ms, total budget widened 10s → 60s. First-success returns immediately so the happy path is unchanged; the wider ceiling tolerates restore's memory-image mmap + page-fault-in cost on n2-standard-4. `pollAPISocketDefault` delegates to the new helper while keeping the seam interface stable for tests. Error message now includes attempts + lastErr (`ch: api socket not responsive at <p> within 60s (attempts=599, lastErr=connect: connection refused)`). Mirrors C-7-LT-2-PR1's compio-native probe pattern on the sandbox-snapshot-restore worktree.
  - **PR2 (`016c8aa1`): per-alloc CH stderr capture.** `ch/restore_task.go` opens `<runDir>/ch-stderr.log` (O_TRUNC per spawn) and sets `cmd.Stderr` before the runner factory wraps it; `defaultRunnerFactory` now tees through `io.MultiWriter` when stderr is pre-set, preserving the in-memory tail buffer (WaitTask exit-tail) while gaining on-disk persistence past task-failure GC. On socket-timeout the error embeds the file's tail (`readStderrTail`, up to 4 KiB) so the next cluster smoke can distinguish CH-crashed from CH-just-slow without losing the alloc dir to GC.
  - Tests: 81 → 87 PASS (+6: `TestWaitForCHSocketReady_{Happy,Timeout,AcceptAtBudgetEdge,EmptyPath}` + `TestStartTaskRestoreBranch_StderrCaptured` + `TestStartTaskRestoreBranch_StderrTailEmbeddedOnSocketTimeout`). go vet + race clean.
  - **Sibling-site audit (per smoke-r13's premise — don't fix what's not the wedge):** the only other `net.Dial("unix", …)` in the driver is `ch/ch_client.go::httpClientForSocket` (the per-RPC HTTP transport CH `vm.info` uses). It's already wrapped in `http.Client{Timeout: 5s}` and is invoked only AFTER the readiness probe declared the socket alive, so it's safe to leave. Cold-boot `start_task.go` has NO equivalent dial — cold-boot spawns CH and trusts the subsequent RPCs (e.g. RecoverTask's `ch-remote info`) to surface a dead socket on demand. Document-only: cold-boot adopting the same readiness probe is a future enhancement, not on the C-7-LT-3 critical path.
  - **Driver v5 upload required for smoke-r15.** Rebuild `dist/nomad-driver-ch` at `016c8aa1`, upload as `nomad-driver-ch.v5`, bump pin in `gcp-worker-startup.sh` v4→v5. No controller change required (C-7-LT-3 is entirely driver-side).
- [x] C-7-LT-4 (restore-path config.json rewriter R15-S2 allow-list) — closes C-7-LT-4 from `docs/reviews/sandbox-snapshot-restore-cluster-2026-05-25-T8b-smoke-r15.md`. Smoke-r15 caught CH `--restore` aborting at +3ms with `CreateConsoleDevice(CreateConsoleDevice(Os{code: 2, kind: NotFound}))`. Root cause: the snapshot's `config.json` carries alloc-scoped paths (e.g. `/opt/nomad/data/alloc/<OLD-id>/ch/local/serial.log`) and the Go driver — which bypasses the bash wrapper — was rewriting the paths but NOT enforcing the wrapper's R15-S2 security allow-list. Any path that didn't match the alloc-prefix regex was silently passed through to CH. Pre-C-7-LT-4 a malicious or malformed snapshot could feed CH `/etc/shadow`-shaped paths and CH would open them verbatim. Fix: `ch/config_rewrite.go::rewriteAndAssertUnderTaskDir` mirrors the bash wrapper's `assert_under_task_dir` at `nomad-vm-wrapper.sh:537-583`. Each path-bearing field (disks[].path, serial.file, console.file, fs[].socket) gets the rewrite-then-validate treatment: post-rewrite path MUST be absolute, MUST NOT contain `..` components, and MUST resolve under the alloc's task_dir. Mismatches surface as `ch: rewriteConfigJSON: disks[0].path = "/etc/shadow" resolves to … NOT under expected prefix …; possible malicious snapshot or misrouted restore` — abort StartTask before CH spawn. Sibling fix C-7-LT-5: `Plugin.SetConfig` was calling `chClient.SetBinaries(chBin, VirtiofsdBin)` — second arg is `chRemoteBin`, not virtiofsd, so `ch-remote` was silently aliased to the virtiofsd binary, breaking the StopTask shutdown step. Added `ch_remote_bin` to Config + hclspec + fixed all three call sites (driver.go:203, start_task.go:141, restore_task.go:302). Tests: 87 → 95 PASS (+8: 5 rewriter allow-list pins covering happy-path, non-path field preservation, security reject across all 4 fields, `..`-traversal reject, missing-optional-fields tolerance + 2 integration pins for the restore branch call site + 1 SetBinaries regression pin). go vet + race clean. NOTE: Driver v6 upload required for smoke-r16: rebuild `dist/nomad-driver-ch`, upload as `nomad-driver-ch.v6`, bump pin in `gcp-worker-startup.sh` v5→v6. Controller change may be needed to populate the new `ch_remote_bin` agent config field (default literal `/usr/local/bin/ch-remote` covers the common case).
- [x] T-8b-driver-v5-upload (2026-05-23) — v5 binary shipped to the cluster pipeline.
  - Build: `nix develop -c bash scripts/build-binary.sh --verify` at HEAD `2623b3e1` (the SPRINT-STATUS-update commit on top of C-7-LT-3-PR2 `016c8aa1`; SPRINT-STATUS.md is not linked into the Go build, so the v5 binary still embeds the C-7-LT-3 fix tree byte-for-byte).
  - Artefact: `dist/nomad-driver-ch`, 20,197,560 bytes (~19.3 MiB), ELF 64-bit LSB statically linked, stripped, `-trimpath`, CGO_ENABLED=0, `-X main.gitSHA=2623b3e1`. SHA256 `c79f3e06dc313355b136e83afc144008a70f3abf576c1b6aa3dd048a2d64105b`. Reproducibility: two back-to-back builds produced byte-identical SHA256 (--verify check passed).
  - Upload: `gsutil cp dist/nomad-driver-ch gs://suger-dev-zsbx-artifacts/nomad-driver-ch.v5`. GCS MD5 `snIgA0zB79zgmv0s3w4OBQ==` matches local md5 (round-trip OK); GCS Content-Length 20197560 matches local size; Generation `1779611080113560`.
  - Pin bump: `crates/sandbox/scripts/gcp-worker-startup.sh` v4→v5 in the sandbox worktree at commit `9b406e04` (branch `feat/sandbox-snapshot-restore`). shellcheck `--severity=error` clean across all 7 scripts (`crates/sandbox/scripts/lint.sh` exit 0).
  - **Ready for T-8b-smoke-r15.** No controller change required.
- [x] C-7-LT-6 (per-field path allow-list in restore-path config rewriter) — closes C-7-LT-6 from `docs/reviews/sandbox-snapshot-restore-cluster-2026-05-25-T8b-smoke-r16.md`. Smoke-r16 caught C-7-LT-4's "all paths under task_dir" invariant rejecting the legitimate per-sandbox persistent workspace disk at `/var/zeroship/ch/<sandbox_id>/workspace.img` — the driver self-aborted in `startTaskRestoreBranch: rewrite config: rewriteConfigJSON: disks[1].path … NOT under expected prefix … (task_dir); possible malicious snapshot or misrouted restore` BEFORE invoking CH. C-7-LT-4 had correctly enforced the bash wrapper's R15-S2 allow-list, but conflated `serial.file`'s alloc-scoped invariant with `disks[*].path`'s persistent-storage layout (the per-sandbox workspace lives OUTSIDE the alloc task_dir by design — it must survive the alloc lifecycle for snapshot/restore). Fix: `ch/config_rewrite.go::validatePathByKind` replaces the single invariant with a per-field allow-list keyed by `PathFieldKind`:
  - `PathFieldRuntimeFile` (serial.file, console.file) — under task_dir only (current rule, retained verbatim).
  - `PathFieldDisk` (disks[*].path) — under per-sandbox prefix (`/var/zeroship/ch/<sandbox_id>/`) OR under task_dir OR under any operator-supplied content-addressed root (read-only base rootfs images).
  - `PathFieldFsSocket` (fs[*].socket) — under task_dir.
  Per-tenant isolation retained: a snapshot that names ANOTHER sandbox's prefix (`/var/zeroship/ch/sbx_OTHER/...`) is rejected — the per-sandbox prefix keys on the CURRENT alloc's sandbox_id, not a wildcard. `..` traversal still rejected as ANY path component. The per-sandbox `/var/zeroship/ch/<sbx>/...` paths do NOT match the alloc-prefix rewriter regex, so they pass through unchanged (preserving the "stable across alloc lifecycle" property smoke-r16 called out). Bash wrapper cross-check: the wrapper at `nomad-vm-wrapper.sh:589-624` enforces strict task_dir for ALL fields including disks — its layout relies on the rootfs being cp'd INTO the alloc and the workspace/userhome paths being supplied via env vars that resolve to alloc-local paths BEFORE the rewriter runs. The Go driver bypasses the wrapper and consumes the snapshot's config.json verbatim, which is where the persistent workspace path needs the new allow-list. The wrapper's implicit layout invariant becomes an explicit per-field validator in the Go port. Plumbing: `Plugin.Config.ContentAddressedRootfsRoots []string` (codec `content_addressed_rootfs_roots`) added to driver config + hclspec; `driverConfig.SandboxId` already plumbed into the restore branch (used for TaskState persistence) — now also passed to `rewriteConfigJSON`. Tests: 95 → 104 PASS (+9: 7 `TestValidatePath_*` allow-list pins covering per-sandbox happy path, task_dir happy path, content-addressed happy path, random-absolute reject, wrong-sandbox per-tenant-isolation reject, serial-file-must-be-task_dir-only, `..` traversal reject; + 2 `TestRewriteRestoreConfigPaths_*` integration pins covering the full smoke-r16 scenario (persistent workspace preserved + serial rewritten to new task_dir) and the per-field dispatch (same prefix accepted for disk, rejected for serial)). go vet + race clean. NOTE: Driver v7 upload required for smoke-r17: rebuild `dist/nomad-driver-ch`, upload as `nomad-driver-ch.v7`, bump pin in `gcp-worker-startup.sh` v6→v7. Operators MAY want to set `content_addressed_rootfs_roots` in the driver agent stanza if they use shared read-only base rootfs images; the default (empty) restricts disks to per-sandbox prefix + task_dir, which covers the smoke-r17 scenario.
- [x] C-7-LT-7 (per-user-home prefix in restore-path config rewriter allow-list) — closes C-7-LT-7 from `docs/reviews/sandbox-snapshot-restore-cluster-2026-05-25-T8b-smoke-r17.md`. Smoke-r17 caught C-7-LT-6's per-field allow-list rejecting the legitimate per-user persistent home disk at `/var/zeroship/ch/users/<user_id>/home.img` — the driver self-aborted on `disks[2].path` (one disk index forward from r16's `disks[1]` workspace failure). The per-user home image is shared across every sandbox a user owns (package caches, dotfiles persist), so it lives OUTSIDE the per-sandbox prefix by design; C-7-LT-6 covered per-sandbox + task_dir + content-addressed roots but missed the per-user namespace. Fix: `ch/config_rewrite.go::validatePathByKind` adds a fourth allow-list slot for `PathFieldDisk`:
  - per-sandbox prefix (`/var/zeroship/ch/<sandbox_id>/`) — C-7-LT-6, retained.
  - **NEW:** per-user home prefix (`/var/zeroship/ch/users/<user_id>/`) — C-7-LT-7, keyed on the CURRENT alloc's user_id.
  - task_dir — C-7-LT-6, retained.
  - operator-supplied content-addressed roots — C-7-LT-6, retained.
  Per-user isolation: a snapshot that names ANOTHER user's home prefix (`/var/zeroship/ch/users/usr_OTHER/...`) is REJECTED — the per-user prefix uses the CURRENT alloc's user_id, not a wildcard. Empty user_id disables the per-user slot entirely (mismatch a home-shaped path → rejected, not silently accepted) — guards against a misconfig where the controller fails to emit user_id. The fix follows approach B from the smoke-r17 review (per-namespace allow-list, layout-stable prefixes). Per-tenant isolation against the sandbox prefix is retained verbatim. `..` traversal still rejected as ANY path component. Plumbing: `TaskConfig.UserId` (codec `user_id`) + hclspec entry added on the driver side; `driverConfig.UserId` is now passed to `rewriteConfigJSON` alongside `SandboxId`. Controller-side (sandbox worktree): the ChPlugin branch of `build_nomad_job_json_with` emits `"user_id": user_id` in the Config block (single line; `user_id` is already a function parameter and is emitted in Meta as `zeroship.user`). Tests: 104 → 110 PASS (+6: `TestValidatePath_UserHomePrefix_OK` happy path; `TestValidatePath_UserHomePrefix_CrossUser_Rejected` per-tenant isolation; `TestValidatePath_UserHomePrefix_EmptyUserId_Rejected` misconfig defence; `TestValidatePath_UserHomePrefix_NotADisk_RejectsAtRuntimeKind` kind-dispatch pin; `TestRewriteRestoreConfigPaths_AcceptsUserHomeAndSandboxAndTaskDir` full integration across all 3 disk-kind paths in one config; `TestUserHomePrefixForTest` layout-prefix pin). go vet + race clean. NOTE: Driver v8 upload required for smoke-r18: rebuild `dist/nomad-driver-ch`, upload as `nomad-driver-ch.v8`, bump pin in `gcp-worker-startup.sh` v7→v8. Controller-side: the new `user_id` field in the ChPlugin Config block is mandatory for the per-user-home slot to activate; the sandbox worktree commit emits it unconditionally.
- [x] C-7-LT-9 (pre-create serial.file/console.file on restore branch) — closes C-7-LT-9 from `docs/reviews/sandbox-snapshot-restore-cluster-2026-05-25-T8b-smoke-r19.md`. Smoke-r19 caught CH `--restore` aborting with `CreateConsoleDevices(CreateConsoleDevice(Os { code: 2, kind: NotFound, message: "No such file or directory" }))` before reaching VmBoot. Root cause: the snapshot's `config.json` carries `serial.file = /opt/nomad/data/alloc/<OLD>/ch/local/serial.log`; the rewriter ALREADY retargets it to `<new_task_dir>/serial.log` (via `rewriteSnapshotPath` — the rewrite branch was correct, the smoke-r19 hypothesis on the missing retarget was wrong), but the NEW task_dir is freshly created and contains no `serial.log` yet (the OLD alloc's file is unreachable). CH opens `serial.file` / `console.file` without `O_CREAT` on the restore path (only `--serial file=...` on cold-boot uses O_CREAT), so ENOENT aborts before VmBoot. Fix shape: `ch/config_rewrite.go::rewriteConfigJSON` now returns a second value — `runtimeFiles []string`, the post-rewrite absolute paths of `serial.file`/`console.file` (PathFieldRuntimeFile kind only; `fs[*].socket` retargets but is deliberately NOT collected — virtiofsd owns the socket lifecycle and a pre-existing regular file there would regress the wake). `ch/restore_task.go::startTaskRestoreBranch` walks the returned slice and `OpenFile(O_WRONLY|O_CREATE|O_TRUNC, 0o640)`+`Close()` each path BEFORE spawning CH. Bash wrapper at `nomad-vm-wrapper.sh` shares the same latent bug (it never touches serial.log either) but the Go driver path is the one production hit in smoke-r19. Tests: 110 → 116 PASS (+6: `TestRewriteRestoreConfigPaths_RetargetsSerialFile` regression pin on the retarget half; `TestRewriteRestoreConfigPaths_RetargetsConsoleFile` symmetric console pin; `TestRewriteRestoreConfigPaths_FsSocketRetargeted_NotPreCreated` asymmetry pin — fs[*].socket rewritten but NOT in runtimeFiles; `TestRewriteRestoreConfigPaths_RuntimeFilesEmptyWhenNoLogs` negative pin — no serial/console → empty slice → pre-create loop no-op; `TestStartTaskRestoreBranch_PreCreatesSerialLog` integration witness — file exists on disk post-StartTask, mode 0o640; `TestStartTaskRestoreBranch_PreCreatesConsoleLog` symmetric integration). go vet + go test clean. NOTE: Driver v9 upload required for smoke-r20: rebuild `dist/nomad-driver-ch`, upload as `nomad-driver-ch.v9`, bump pin in `gcp-worker-startup.sh` v8→v9. No controller change required (the fix is entirely driver-side).
- [x] C-7-LT-10 (route CH --restore through runDir, not the source dir) — `1d5aa2f9` — closes C-7-LT-10 from `docs/reviews/sandbox-snapshot-restore-cluster-2026-05-25-T8b-smoke-r20.md`. Three-cycle defect class. Smoke-r20 source-audit revealed a writer/reader asymmetry inside `ch/restore_task.go::startTaskRestoreBranch`: the rewriter wrote the path-rewritten `config.json` into `<runDir>/config.json` (line :319), but the CH invocation passed `--restore source_url=file://<RestoreFrom>` (line :420). CH dutifully read the un-rewritten `config.json` from the snapshot source dir, ignoring the rewriter output entirely — the source dir still carried the OLD alloc's `serial.file = /opt/nomad/data/alloc/<OLD>/ch/local/serial.log`, so CH opened that stale path and aborted at `CreateConsoleDevice ENOENT` before VmBoot. Three smoke cycles (r15/r19/r20) all hit the same ENOENT; the C-7-LT-4 (rewriter R15-S2 allow-list) and C-7-LT-9 (pre-create) fixes were correct but invisible because their output never reached CH. Fix shape (Shape A per the smoke-r20 review): route CH through runDir for all three files. Symlink `state.json` + `memory-ranges` from `<RestoreFrom>` into `<runDir>` (the immutable snapshot artifacts CH consumes byte-for-byte stay in the read-only source dir, only referenced via the symlinks), and change the invocation to `--restore source_url=file://<runDir>`. The rewritten `config.json` already in runDir is now the source of truth. Symlink creation tolerates `fs.ErrExist` so a re-attempt of a previously-failed restore is idempotent. The read-only source dir is still never written to. Bash wrapper cross-check: the wrapper at `nomad-vm-wrapper.sh:474-679` rewrites `config.json` IN PLACE inside `$ZSBX_RESTORE_FROM` and then passes `source_url=file://$ZSBX_RESTORE_FROM` — single-dir model, no symlinks needed. The Go driver deliberately writes into runDir (preserves the read-only contract on the source dir) and now achieves the same single-dir model via symlinks. Lesson committed to the commit message: source-audit BOTH writer AND reader before publishing a fix — the 3-cycle cost (r15/r19/r20) demonstrates the discipline gap. Tests: 116 → 120 PASS (+4: `TestStartTaskRestoreBranch_PassesRunDirToCH` argv pin — `--restore` arg ends with runDir, NOT staged source dir; `TestStartTaskRestoreBranch_SymlinksImmutableArtifacts` — state.json + memory-ranges are symlinks under runDir resolving to the source dir; `TestStartTaskRestoreBranch_SymlinkIdempotentOnRetry` — pre-staged symlinks don't error on the second call; `TestStartTaskRestoreBranch_RewriteConfigAndSymlinksCoexist` — config.json regular file + state.json/memory-ranges symlinks all under runDir in one StartTask). The existing `TestStartTaskRestore_SpawnsWithRestoreFlag` argv pin was updated: `staged` suffix → `runDir` suffix to match the new contract. go vet + go test clean. NOTE: Driver v10 upload required for smoke-r21: rebuild `dist/nomad-driver-ch`, upload as `nomad-driver-ch.v10`, bump pin in `gcp-worker-startup.sh` v9→v10. No controller change required (the fix is entirely driver-side: symlink the immutable artifacts into runDir before CH spawn, point `--restore` at runDir).
- [x] T-8b-driver-v9-upload (2026-05-23) — v9 binary shipped to the cluster pipeline (C-7-LT-9 pre-create serial.file/console.file on restore).
  - Build: `nix develop -c bash scripts/build-binary.sh --verify` at HEAD `00706d29` (the SPRINT-STATUS-update commit on top of C-7-LT-9; SPRINT-STATUS.md is not linked into the Go build, so the v9 binary embeds the C-7-LT-9 fix tree byte-for-byte).
  - Artefact: `dist/nomad-driver-ch`, 20,209,848 bytes (~19.3 MiB), ELF 64-bit LSB statically linked, stripped, `-trimpath`, CGO_ENABLED=0, `-X main.gitSHA=00706d29`. SHA256 `7c45cdc06d42e58df8cc943ac5eac12775f0531a4fd723ccfd43ea7d6da337d2`. Reproducibility: two back-to-back builds produced byte-identical SHA256 (--verify check passed).
  - Upload: `gsutil cp dist/nomad-driver-ch gs://suger-dev-zsbx-artifacts/nomad-driver-ch.v9`. GCS MD5 hex `6addf7afbd1aa8d68c6e703c4be66c34` matches local md5 (round-trip OK); GCS crc32c `cbba09b9`.
  - Pin bump: `crates/sandbox/scripts/gcp-worker-startup.sh` v8→v9 in the sandbox worktree at commit `f2641e88` (branch `feat/sandbox-snapshot-restore`). shellcheck `--severity=error` clean across all 7 scripts (`crates/sandbox/scripts/lint.sh` exit 0).
  - **Ready for T-8b-smoke-r20.** No controller change required (the fix is entirely driver-side: rewriter now returns `runtimeFiles`, startTaskRestoreBranch pre-creates each path with `OpenFile(O_WRONLY|O_CREATE|O_TRUNC, 0o640)` + `Close()` before CH spawn). Controller v31 unchanged.
- [x] T-8b-driver-v8-upload (2026-05-23) — v8 binary shipped to the cluster pipeline (C-7-LT-7 per-user-home allow-list slot).
  - Build: `nix develop -c bash scripts/build-binary.sh --verify` at HEAD `f329ba14` (the SPRINT-STATUS-update commit on top of C-7-LT-7; SPRINT-STATUS.md is not linked into the Go build, so the v8 binary embeds the C-7-LT-7 fix tree byte-for-byte).
  - Artefact: `dist/nomad-driver-ch`, 20,205,752 bytes (~19.3 MiB), ELF 64-bit LSB statically linked, stripped, `-trimpath`, CGO_ENABLED=0, `-X main.gitSHA=f329ba14`. SHA256 `527bf8301da5767cec8fb848f9122b2d330c082938a36cc41097aeb31e7e3388`. Reproducibility: two back-to-back builds produced byte-identical SHA256 (--verify check passed).
  - Upload: `gsutil cp dist/nomad-driver-ch gs://suger-dev-zsbx-artifacts/nomad-driver-ch.v8`. GCS MD5 `vufFMFAXw76lbG/Mv2mSUQ==` (hex `bee7c5305017c3bea56c6fccbf699251`) matches local md5 (round-trip OK); GCS crc32c `4405e331`.
  - Pin bump: `crates/sandbox/scripts/gcp-worker-startup.sh` v7→v8 in the sandbox worktree at commit `4d73a5d1` (branch `feat/sandbox-snapshot-restore`). shellcheck `--severity=error` clean across all 7 scripts (`crates/sandbox/scripts/lint.sh` exit 0).
  - **Ready for T-8b-smoke-r18.** Controller-side prerequisite is already satisfied: the sandbox worktree at `03d2f4a8` emits `user_id` in the ChPlugin Config block, which the v8 driver consumes to activate the per-user-home allow-list slot.
- [x] T-8b-driver-v7-upload (2026-05-23) — v7 binary shipped to the cluster pipeline (C-7-LT-6 per-field allow-list).
  - Build: `nix develop -c bash scripts/build-binary.sh` at HEAD `f41a869a` (the SPRINT-STATUS-update commit on top of C-7-LT-6; SPRINT-STATUS.md is not linked into the Go build, so the v7 binary embeds the C-7-LT-6 fix tree byte-for-byte).
  - Artefact: `dist/nomad-driver-ch`, 20,205,752 bytes (~19.3 MiB), ELF 64-bit LSB statically linked, stripped, `-trimpath`, CGO_ENABLED=0, `-X main.gitSHA=f41a869a`. SHA256 `c33ace1bdd8a432868e1c7b67d747575e7ecc042a7727282d9613e8f2ed65c9c`. Reproducibility: two back-to-back builds produced byte-identical SHA256.
  - Upload: `gsutil cp dist/nomad-driver-ch gs://suger-dev-zsbx-artifacts/nomad-driver-ch.v7`. GCS MD5 `5eo3jJhqULobyOxgiiMRvg==` (hex `e5ea378c986a50ba1bc8ec608a2311be`) matches local md5 (round-trip OK); GCS Content-Length 20205752 matches local size.
  - Pin bump: `crates/sandbox/scripts/gcp-worker-startup.sh` v6→v7 in the sandbox worktree at commit `8718120b` (branch `feat/sandbox-snapshot-restore`). shellcheck `--severity=error` clean across all 7 scripts (`crates/sandbox/scripts/lint.sh` exit 0).
  - **Ready for T-8b-smoke-r17.** No controller change strictly required — the new `content_addressed_rootfs_roots` driver-config attribute defaults to empty (per-sandbox prefix + task_dir cover the smoke-r17 scenario). Operators MAY set it in the driver agent stanza if they use shared read-only base rootfs images.
- [x] T-8b-driver-v6-upload (2026-05-23) — v6 binary shipped to the cluster pipeline (C-7-LT-4 + C-7-LT-5).
  - Build: `nix develop -c bash scripts/build-binary.sh --verify` at HEAD `1fb198b3` (the SPRINT-STATUS-update commit on top of C-7-LT-5 `cffb5c8e` and C-7-LT-4-PR1 `c1df13d3`; SPRINT-STATUS.md is not linked into the Go build, so the v6 binary embeds the C-7-LT-4 + C-7-LT-5 fix tree byte-for-byte).
  - Artefact: `dist/nomad-driver-ch`, 20,197,560 bytes (~19.3 MiB), ELF 64-bit LSB statically linked, stripped, `-trimpath`, CGO_ENABLED=0, `-X main.gitSHA=1fb198b3`. SHA256 `b7982997545525fa80144babd55688c39b384a1f3e3fb6ab01edf7ed204d4655`. Reproducibility: two back-to-back builds produced byte-identical SHA256 (--verify check passed).
  - Upload: `gsutil cp dist/nomad-driver-ch gs://suger-dev-zsbx-artifacts/nomad-driver-ch.v6`. GCS MD5 `Ha7zngzgUjGMjvUajIvZsQ==` matches local md5 (round-trip OK); GCS Content-Length 20197560 matches local size.
  - Pin bump: `crates/sandbox/scripts/gcp-worker-startup.sh` v5→v6 in the sandbox worktree at commit `dea68995` (branch `feat/sandbox-snapshot-restore`). shellcheck `--severity=error` clean across all 7 scripts (`crates/sandbox/scripts/lint.sh` exit 0).
  - **Ready for T-8b-smoke-r16.** No controller change strictly required — the new `ch_remote_bin` driver-config attribute defaults to `/usr/local/bin/ch-remote`, which matches the cluster's existing `nomad-vm-wrapper.sh` layout. Operators only need to override it if their ch-remote path differs.
- [x] T-7 (Controller integration: SANDBOX_TASK_DRIVER=ch_plugin flag) — sandbox worktree `5fe36805` (`sandbox/nomad-ch: SANDBOX_TASK_DRIVER=ch_plugin flag switches jobspec to typed Go driver (T-7)`)
  - Controller's `crates/sandbox/src/backend/nomad_ch.rs::build_nomad_job_json` now branches on env flag: default keeps `Driver: "raw_exec"`; `ch_plugin` emits `Driver: "ch"` + typed `Config{}` mapped to `nomad-driver-ch/ch/task_config.go::TaskConfig`.
  - Field mapping verified against `task_config.go` codec tags: vm_index, kernel, cpus, memory_mb, restore_from, sandbox_id, workspace_img, user_home_img, pubkey_hex, subnet_base_octet, disks/fs/net.
  - Sandbox-crate lib tests: 312 → 319 (+7 T-7 tests). Driver tests unaffected (separate worktree at HEAD `3531a58b`, 63 PASS + 0 SKIP).
  - Production default unchanged — flag stays off until T-8 cluster validation.
- [x] C-7-LT-11 (capture CH stderr on resume-failure branch) — closes C-7-LT-11 from smoke-r21 review. Smoke-r21 confirmed CH `--restore` succeeds (3-cycle defect class CLOSED) but `ch-remote resume` returned HTTP 500 "VM is not running" with no diagnostic data — the driver killed the VM and emitted only the terse resume-failed error. C-7-LT-3-PR2 (`016c8aa1`) already added stderr capture for the socket-poll-timeout branch; C-7-LT-11 mirrors that 5-LOC pattern for the resume-failure branch in `ch/restore_task.go:542-556`. `readStderrTail` (on-disk file) with fallback to `runner.StderrTail` (in-memory buffer); tail embedded in the error as `ch_stderr_tail=<q>` when non-empty, otherwise `(no ch stderr captured; path=<p>)`. Unit test `TestStartTaskRestoreBranch_StderrTailEmbeddedOnResumeFail` mirrors `TestStartTaskRestoreBranch_StderrTailEmbeddedOnSocketTimeout` — `SetResumeForTest` seam injects a marker into the stderr file then returns `ch-remote resume: HTTP 500: VM is not running`; asserts marker + `ch_stderr_tail=` + path surface in the error. Tests: 120 → 121 PASS. go vet clean. Driver v11 upload pending.

## In progress

- T-8a (deploy artifacts staged) — driver side landed; sandbox-side commit lands in the sandbox worktree under the same sprint header
  - `scripts/build-binary.sh` produces `dist/nomad-driver-ch` (stripped, CGO_ENABLED=0, `-trimpath`, `-X main.gitSHA=<short-sha>`). Verified reproducible: SHA stable across rebuilds of the same commit.
  - `scripts/upload-to-gcs.sh <tag>` is a dry-run printer — emits the `gcloud storage cp` invocation but never runs it (cost-control until release pipeline lands).
  - `cmd/nomad-driver-ch/main.go`: `var gitSHA = "dev"` overridable via `-ldflags="-X main.gitSHA=<sha>"`; `--version` prints `nomad-driver-ch <sha>` and exits 0 before plugins.Serve.
  - Tests: 63 → 64 PASS (+1 = `TestVersionFlag` exercises default + ldflags-override builds).
  - Static-link check: `file dist/nomad-driver-ch` → `... statically linked, ... stripped`. Size 19.3 MB (well under the 28 MB sprint upper bound).
  - Live cluster smoke is T-8b, dispatched separately on explicit OK.

## Up next

- T-8b-smoke (2026-05-25): **FAIL — NO-GO for T-8b-stress.** Single 1-server+1-worker cluster came up cleanly in 105s; binary `nomad-driver-ch.v1` (sha `df2b117511e9f218ca20ccd3ce74e1c2d37c65cfb8deeeca4b9b70d9621a25f8`, gitSHA `ce118450`) uploaded and deployed to `/etc/zeroship/nomad-plugins/`; `zsbx-ctl` Environment contains `SANDBOX_TASK_DRIVER=ch_plugin`. But the snap/wake cycle failed at CREATE with `nomad alloc terminal status=failed: Failed tasks`. Two independent issues uncovered: (a) Nomad refused to load the plugin — `plugin not referenced in the agent configuration file, loading skipped` — `/etc/nomad.d/plugin-dir.hcl` needs a `plugin "nomad-driver-ch" { config {} }` stanza, not just `plugin_dir`; (b) the controller pin `zeroship-sandbox.snapshot-v6` predates `a4c481e1` (B24 `ZSBX_SANDBOX_ID` injection) and `5fe36805` (T-7 jobspec switch), so the controller (i) silently ignored `SANDBOX_TASK_DRIVER` and emitted raw_exec jobs and (ii) killed every alloc via the wrapper's R8-DEPLOY1 guard. The Go driver was never reached. Teardown clean (0 residual). Cost ≈ $0.25. Full report: `docs/reviews/sandbox-snapshot-restore-cluster-2026-05-25-T8b-smoke-r1.md` in the sandbox worktree.

- T-8b retry (blocked on): (1) bake & upload a fresh controller binary including `a4c481e1` + `5fe36805`, bump `CONTROLLER_OBJECT` default in `provision-gcp-cluster.sh`; (2) add `plugin "nomad-driver-ch" { config {} }` stanza to `gcp-worker-startup.sh:185-188`. Both pre-reqs live in the sandbox worktree.

- T-8b-smoke retry r2 (2026-05-25): cluster r2 caught C-1 (CH `--config` argv bug) — fix landed in this worktree. Driver v3 upload sprint pending: rebuild `dist/nomad-driver-ch` at the C-1 commit, upload as `nomad-driver-ch.v3`, bump pin in `gcp-worker-startup.sh` from v2→v3. **NO controller change required** (the C-1 fix is entirely driver-side). After v3 lands, dispatch T-8b-smoke-retry-3.

- T-8b-smoke retry r3 (2026-05-25): cluster r3 caught C-2 (disk path / rootfs staging bug) — fix landed in this worktree at `f521eb21`. Driver v4 upload sprint pending: rebuild `dist/nomad-driver-ch` at the C-2 commit (`f521eb21`), upload as `nomad-driver-ch.v4`, bump pin in `gcp-worker-startup.sh` from v3→v4. **NO controller change required** (the C-2 fix is entirely driver-side; the controller already emits `ZSBX_ARTIFACT_DIR` env var on every job per `crates/sandbox/src/backend/nomad_ch.rs:2339`+`l.2495`, the driver now consumes it). After v4 lands, dispatch T-8b-smoke-retry-r4 — a 1-worker smoke that should reach `CREATE OK: 1/1` end-to-end since CH will now find the rootfs.img at the path the driver passes.

- T-8b-smoke retry r16 (2026-05-23): cluster r15 caught C-7-LT-4 (restore config.json security allow-list) + C-7-LT-5 (SetBinaries argument shape — virtiofsd was aliased to ch-remote). Both fixes landed in this worktree. Driver v6 upload sprint pending: rebuild `dist/nomad-driver-ch` at the C-7-LT-4 commit, upload as `nomad-driver-ch.v6`, bump pin in `gcp-worker-startup.sh` from v5→v6. Controller-side: the new `ch_remote_bin` driver-config attribute has a sensible default (`/usr/local/bin/ch-remote`) — operators only need to override it if their layout differs. If the cluster's `gcp-worker-startup.sh` agent stanza already writes `plugin "nomad-driver-ch" { config { … } }` it can stay; if not, the default is sufficient. After v6 lands, dispatch T-8b-smoke-retry-r16 — the wake-from-snapshot path should now (a) reject malicious snapshots before CH spawn and (b) issue ch-remote shutdown via the correct binary, eliminating the SIGTERM-fallback degradation observed in r15.

- T-8b-smoke retry r17 (2026-05-23): cluster r16 caught C-7-LT-6 (path rewriter's "all paths under task_dir" invariant rejected legitimate per-sandbox persistent workspace.img at `/var/zeroship/ch/<sbx>/workspace.img`). Fix landed in this worktree: `ch/config_rewrite.go` now uses a per-field allow-list (PathFieldRuntimeFile under task_dir only; PathFieldDisk under per-sandbox prefix OR task_dir OR content-addressed root; PathFieldFsSocket under task_dir). Driver v7 upload sprint pending: rebuild `dist/nomad-driver-ch` at the C-7-LT-6 commit, upload as `nomad-driver-ch.v7`, bump pin in `gcp-worker-startup.sh` from v6→v7. Controller-side: optional `content_addressed_rootfs_roots` driver-config attribute is empty by default (per-sandbox + task_dir cover the smoke-r17 scenario). After v7 lands, dispatch T-8b-smoke-retry-r17 — the wake-from-snapshot path should now reach `livez_polling` for the first time (CH actually starts, agent comes up, R19-I1 probe runs).

- T-8b-smoke retry r18 (2026-05-23): cluster r17 caught C-7-LT-7 (C-7-LT-6's per-field allow-list was one entry short — missing the per-user persistent home prefix `/var/zeroship/ch/users/<user_id>/home.img`). Fix landed in this worktree: `ch/config_rewrite.go` now whitelists per-user-home alongside per-sandbox/task_dir/content-addressed, keyed on the new `TaskConfig.UserId` field. Driver v8 upload sprint pending: rebuild `dist/nomad-driver-ch` at the C-7-LT-7 commit, upload as `nomad-driver-ch.v8`, bump pin in `gcp-worker-startup.sh` from v7→v8. Controller-side: a single-line change in the sandbox worktree adds `"user_id": user_id` to the ChPlugin `Config` block (the parameter was already plumbed in; we just weren't emitting it on the wire). After v8 lands, dispatch T-8b-smoke-retry-r18 — predictions: rewriter accepts all `disks[0..N].path` (rootfs/workspace/home), state machine reaches `livez_polling` and then `ok` (THE MILESTONE), R19-I1 two-phase livez probe exercised for the first time in production.

- T-8b-smoke retry r20 (2026-05-23): cluster r19 caught C-7-LT-9 (CH `--restore` ENOENT-aborts at `CreateConsoleDevices` because the rewritten `serial.file` path doesn't yet exist in the new task_dir — CH opens without O_CREAT on the restore branch). Fix landed in this worktree at `742c43e4`: `ch/config_rewrite.go::rewriteConfigJSON` now returns the post-rewrite runtime-file paths (`serial.file` + `console.file`) and `ch/restore_task.go::startTaskRestoreBranch` `touch`-creates each with mode 0o640 BEFORE spawning CH. `fs[*].socket` retargets but is NOT pre-created (virtiofsd owns the socket). Driver v9 upload sprint pending: rebuild `dist/nomad-driver-ch` at C-7-LT-9, upload as `nomad-driver-ch.v9`, bump pin in `gcp-worker-startup.sh` v8→v9. No controller change required. After v9 lands, dispatch T-8b-smoke-retry-r20 — prediction: CH `--restore` reaches VmBoot (no more CreateConsoleDevices abort), guest userspace comes up, agent's /livez probe returns 200 on first attempt of the two-phase loop.

- T-8b-smoke retry r21 (2026-05-23): cluster r20 source-audit caught C-7-LT-10 (driver wrote rewritten config.json to runDir but invoked CH `--restore source_url=file://<RestoreFrom>` — CH read the un-rewritten config from the snapshot dir, so the C-7-LT-4 rewriter and C-7-LT-9 pre-create fixes were correct but never reached CH). Three-cycle defect class: r15/r19/r20 all hit the same `CreateConsoleDevice ENOENT` because of this writer/reader asymmetry. Fix landed in this worktree at `1d5aa2f9`: symlink `state.json` + `memory-ranges` from `<RestoreFrom>` into `<runDir>`, then pass `--restore source_url=file://<runDir>` to CH. The rewritten config.json in runDir is now the source of truth; immutable artifacts are symlinked (no write to the read-only snapshot dir). Driver v10 upload sprint pending: rebuild `dist/nomad-driver-ch` at C-7-LT-10, upload as `nomad-driver-ch.v10`, bump pin in `gcp-worker-startup.sh` v9→v10. No controller change required. After v10 lands, dispatch T-8b-smoke-retry-r21 — prediction: CH `--restore` actually consumes the rewritten config.json for the first time across all three cycles, reaches VmBoot, guest userspace comes up, agent's /livez probe returns 200 — THE milestone the C-7-LT-9 prediction missed because the fix was invisible.

- T-8b-driver-v10-upload (2026-05-23): **COMPLETE.** Driver v10 built reproducibly from gitSHA `1b6ab161` (C-7-LT-10 — `restore_task.go` routes CH `--restore source_url` through runDir; immutable snapshot artifacts symlinked). SHA256 `09125a5ca072038d93221f25bfc68549499030bbeb68f8eb81fcd37b18850a14`, size 20,209,848 B (19.3 MiB), statically linked + stripped. `scripts/build-binary.sh --verify` confirmed bit-identical rebuild. Uploaded to `gs://suger-dev-zsbx-artifacts/nomad-driver-ch.v10`; GCS MD5 `75e5e3c398d9e36e14edfcd90cde6e42` matches local digest (round-trip verified). Sandbox-side pin bump v9→v10 committed in the sandbox worktree at `1da1e4e9` (`gcp-worker-startup.sh:176`); `lint.sh` exit 0. T-8b-smoke-r21 cluster cycle dispatched next — first end-to-end exercise of the runDir-rooted `--restore` flow.

- T-8b-driver-v11-upload (2026-05-25): **COMPLETE.** Driver v11 built reproducibly from gitSHA `446f1529` (C-7-LT-11 — resume-failure branch now captures `ch_stderr_tail=<...>` from the CH process before returning the error; diagnostic for C-7-LT-12 CH-internal "VM is not running" post-restore). SHA256 `f6ff6f218632fe87af43186edbfef86c8ddbe9f0306a163e45256c80628e6224`, size 20,209,848 B (19.3 MiB), statically linked + stripped. `scripts/build-binary.sh --verify` confirmed bit-identical rebuild. Uploaded to `gs://suger-dev-zsbx-artifacts/nomad-driver-ch.v11`; GCS MD5 `95dec321fd6243c46ff3517dff62120d` matches local digest (round-trip verified). Sandbox-side pin bump v10→v11 committed in the sandbox worktree at `8b366b6d` (`gcp-worker-startup.sh:176`); `lint.sh` exit 0. T-8b-smoke-r22 cluster cycle dispatched next — primary success criterion is capturing verbatim CH stderr tail from the resume-failure branch (the diagnostic gap r21 exposed).

- T-8b-stress: gated on a clean T-8b-smoke. Out of scope until smoke passes.

- T-8b-cutover (wrapper removal): gated on T-8b-stress. Out of reach.
