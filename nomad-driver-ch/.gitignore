# Build artefacts
/bin/
# scripts/build-binary.sh drops the release artefact here.
/dist/
# Ad-hoc `go build .` or `go build ./cmd/nomad-driver-ch` output dropped at
# the module root. Anchored with a leading slash so it does NOT match the
# `cmd/nomad-driver-ch/` source directory.
/nomad-driver-ch
*.test
*.out
*.log

# Editor / IDE
.idea/
.vscode/
*.code-workspace
*.swp
