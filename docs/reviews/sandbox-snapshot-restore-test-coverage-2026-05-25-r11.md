# Sandbox/snapshot-restore — test-coverage r11 review

Date: 2026-05-25 (UTC)
HEAD at audit: 4f441a20
Round 11 of N (test-coverage lens).

## Summary

- 5 findings (1 critical, 3 important, 1 minor) — all NEW since r10
- Test counts at HEAD: sandbox lib **310 passed / 1 ignored** (was 303/1 at r10 start); sandbox-agent lib **240 passed / 0 ignored** (was 239/0 at r10 start). Aggregate delta this cycle: **+7 sandbox lib, +1 sandbox-agent = +8** (down from r10's +30). Trend over two cycles (r9 → r10 → r11): sandbox 296 → 303 → 310 (+14); sandbox-agent 231 → 239 → 240 (+9). Six R-tickets closed in the r10 cycle; FOUR additional closures in the r11 cycle (R7-API2 doc-regression, R10-Q1 err_safe sentinels, R9-S4b persist.rs uid, R9-S4c db.rs enforce_password_file_mode uid). The r11 cycle's growth was **100% unit-tests in `src/`** — the integration-test surface (`crates/sandbox/tests/`, `crates/sandbox-agent/tests/`) was untouched (`git diff 040812f2 4f441a20 --stat` shows zero changes there).
- Recently landed since r10 (040812f2 → 4f441a20): 4f441a20 reviewer artifacts; 1d1a7c70 docs (R9-S4c CLOSED stamp + new R9-S4d entry); 2c10f63a R9-S4c db.rs uid (+2 tests); c8000537 R7-API2 capability doc-regression (+1 test); d2cfcb34 docs (R10-Q1 CLOSED); 228569d3 R10-Q1 err_safe sentinels (+3 tests); 5f030871 docs (R9-S4b closed stamp); e4e5db60 R9-S4b persist.rs uid (+2 tests).

## Findings (NEW since r10)

### [R11-T1] Cycle drained four MEDIUM tickets but ZERO progress on R10-T1's end-to-end-`restore_sandbox` gap; `let _ = spawn_blocking(…).await` (line 294) introduced in the r10 cycle has zero panic-handling path and zero test (CRITICAL, test-coverage-r11)

- **Files**: `crates/sandbox/src/restore_handler.rs:291-298` (the R10-C2 rollback-closure spawn_blocking — line 294 `let _ = compio::runtime::spawn_blocking(...).await;` discards the `JoinHandle`); `crates/sandbox/src/restore_handler.rs:2327-2360` (the only R10-C2 test — text-grep via `include_str!`, no behaviour); `crates/sandbox/tests/sandbox_pg_e2e.rs:2707,2757,2785,3237,3359` (every pg-e2e `restore_sandbox` call site at HEAD); diff `git log --oneline 040812f2..4f441a20 -- crates/sandbox/tests crates/sandbox-agent/tests` returns empty.
- **Symptom**: r10's R10-T1 explicitly asked for "one integration test that drives `restore_sandbox` end-to-end through the rollback path with a `StubRestoreBackend::fail_submit=true`". The r11 cycle landed `R7-API2` (capability doc-regression), `R10-Q1` (err_safe sentinels), `R9-S4b` (persist.rs uid), `R9-S4c` (db.rs uid) — none of which touched `restore_sandbox`. At HEAD, the FIVE pg-e2e callsites of `restore_sandbox` use only `fail_reserve=true` (line 2781), which trips the early `VmIndexUnavailable` arm at `do_restore_inner` BEFORE the spawn_blocking-wrapped rollback closure at lines 263-313 is reachable. `StubRestoreBackend` has `fail_submit` / `fail_livez` fields (lines 728-729) but ZERO test sets them and then calls `restore_sandbox`. Compounding this, the new R10-C2 spawn_blocking site at line 294 uses `let _ = compio::runtime::spawn_blocking(move || { backend.teardown_restore(...) }).await;` — a JoinHandle-discarding form that does NOT match the explicit panic-conversion pattern at lines 432/522/540/1667 (`.unwrap_or_else(|p| Err(format!("spawn_blocking panic: {p:?}")))`). A panic inside `teardown_restore` would surface as a discarded `_`, NOT as the "spawn_blocking panic: …" prefix in the controller error stream. R9-T3's panic-recovery gap (now SEVEN sites with explicit conversion + this 8th unhandled site) drifted further this cycle, not closer.
- **Why it matters**: the R10-C1 + R10-C2 fixes were the ONLY two CRITICAL concurrency items resolved in the r10 cycle, and their stated invariants (state-map cleanliness + worker-park immunity under rollback) remain pinned only at the callee level (`backend.teardown_restore(...)` called directly at `restore_handler.rs:2235,2308`). A regression where someone removes the spawn_blocking wrap AND changes the anchor comment "Best-effort teardown of the partially-spawned alloc." in the same commit — the R10-C2 grep at line 2336 would `panic!("R10-C2 anchor comment moved; update the test")`, which is a *failure mode that asks the contributor to change the test rather than fix the code*. A behavioural test that wires `StubRestoreBackend::fail_submit=true` through `restore_sandbox` and asserts (a) `teardown_called=true`, (b) row reaches `Snapshotted`, (c) JoinHandle's panic-result is surfaced (or at least logged) — would be invariant-pinned, not text-anchor-pinned. The R10-C2 site's discarded-JoinHandle is a second-order gap from the r10 fix that was flagged in r10's "Notes for the next round" but landed unaddressed.
- **Action**: ONE pg-e2e integration test (`restore_sandbox_rollback_after_submit_failure_via_teardown_restore`) that constructs `StubRestoreBackend { fail_submit: true, .. }`, drives `restore_sandbox(&db, store, backend, None, sid, true).await`, then asserts (a) row.status == `Snapshotted` (rollback CAS landed); (b) `backend.teardown_called.load(SeqCst) == true`; (c) `backend.released.lock().contains(&vm_index)` — i.e. release_vm_index fired through teardown_restore's path; (d) artifact still present on disk. Same shape as `restore_handler_snapshot_corrupt_marks_suspect` at line 2724 but flipping `fail_reserve` → `fail_submit`. Companion test (`restore_sandbox_rollback_after_livez_failure`) for `fail_livez=true`. Both ~40 LOC; mirror the existing pattern. SEPARATELY, address the line-294 panic-handling gap by either (1) attaching `.unwrap_or_else(|p| tracing::error!("teardown spawn_blocking panic: {p:?}"))` to the discard or (2) at minimum filing a follow-up R11-C1-FOLLOWUP marker.

### [R11-T2] R10-T2 carry-forward (Persist(Some(_)) AEAD + clock_resync + register_restored chain) zero progress in r11 cycle — re-files as a third-round CRITICAL (CRITICAL, test-coverage-r11, third-round carry of R9-T1 / R10-T2)

- **Files**: `crates/sandbox/src/restore_handler.rs:581-617` (the `if let Some(p) = persist` chain — unseal + clock_resync + register_restored); `crates/sandbox/tests/sandbox_pg_e2e.rs:2707,2757,2785,3237,3359` (every callsite STILL passes `None` for persist — verified empty `git show HEAD:crates/sandbox/tests/sandbox_pg_e2e.rs | grep -n "persist:\|AeadSnapshotStore\|Persistence::"`).
- **Symptom**: the r11 cycle added +8 tests (sandbox+sandbox-agent combined). None of them is in `crates/sandbox/tests/`. The `Persist(Some(_))` codepath that B22 (post-wake 401 / clock-skew) and B19 (state-map gap) BOTH lived in remains 100% uncovered end-to-end. Of the FOUR closures this cycle, three are uid hardening (R9-S4b/c) and one is err_safe sanitisation (R10-Q1) — orthogonal to the wake-path persist chain. The deferred AEAD-wake-regression (R9-P1: 3 I/O passes on memory-ranges) is still uncovered by any pg-test because no pg-test ever runs wake against an AEAD-wrapped store. This finding is now in its THIRD consecutive round without progress.
- **Why it matters**: the persist=Some chain at lines 581-617 contains the load-bearing post-livez sequence (unseal → clock_resync → register_restored). A future regression that, e.g., swaps clock_resync and register_restored ordering — would land green because every pg-test path hits the `None` branch at lines 607-617 instead. The state-map invariant that R10-C1's pure unit tests pin at the callee level depends on `register_restored` actually being called with the right args in the right order — none of which is exercised through `restore_sandbox`. Both R10-T1 (above) and R10-T2 (here) share the same fundamental pattern: r11's growth was at the unit level, but the structural gap is at the integration level.
- **Action**: unchanged from R10-T2 / R9-T1. ONE pg-gated test wiring `restore_sandbox` with (a) `AeadSnapshotStore::new(LocalDiskSnapshotStore, Some(kek))` for `store`, (b) a real `Persistence` (seal a fake signing key), (c) a fake-agent endpoint that the controller's `clock_resync_post_restore` can POST to (use the existing `spawn_fake_agent`-style harness from the new `r10_c1_*` tests if available, otherwise spawn an ntex listener accepting POST /_clock_resync). Assert: (i) row reaches Running; (ii) register_restored invoked exactly once with the sealed key; (iii) on-disk artifact ciphertext starts with `ZSBXAEAD` magic; (iv) fake agent recorded exactly one POST to `/_clock_resync`. Same shape as snapshot↔restore round-trip at `tests/sandbox_pg_e2e.rs:3001-3119`, but with `persist=Some(...)` instead of `None`.

### [R11-T3] R7-API2's mandatory-capability regression test pins ONLY `clock.resync-v1`; the doc's "feature-detected" caps (`proxy.ws-v1`, `auth.ed25519-v1.1`) have no equivalent (IMPORTANT, test-coverage-r11)

- **Files**: `crates/sandbox-agent/src/version.rs:175-190` (`mandatory_clock_resync_v1_present` — the single new test landed in r11 at `c8000537`); `crates/sandbox-agent/src/version.rs:48-58` (the doc rewrite explicitly naming `proxy.ws-v1` and `auth.ed25519-v1.1` as "genuinely feature-detected by the controller"); `crates/sandbox-agent/src/version.rs:155-172` (the pre-existing `known_capabilities_present` test — pins only 5 baseline caps: `exec`, `files.crud`, `files.tree`, `fs.no-symlink-escape`, `auth.ed25519-v1`, none of the feature-detected ones).
- **Symptom**: R7-API2's doc rewrite at lines 48-58 explicitly bifurcates the CAPABILITIES list into "feature-detected" (mentions `proxy.ws-v1`, `auth.ed25519-v1.1`) vs. "mandatory" (mentions `clock.resync-v1`). The new test only guards the mandatory side. A regression that drops `"proxy.ws-v1"` or `"auth.ed25519-v1.1"` from CAPABILITIES would land green because (a) `capabilities_nonempty_and_unique` only checks set semantics, (b) `capability_strings_are_well_formed` only checks shape, (c) `known_capabilities_present` doesn't list them, (d) the new `mandatory_clock_resync_v1_present` only names clock.resync-v1. This is asymmetric: the doc-rewrite ELEVATED the contract by naming three semantic tiers (feature-detected / mandatory / baseline), but the regression-pin test count went up by ONE. The doc claim "controller does NOT branch on their presence" for the mandatory tier needs the inverse pin for the feature-detected tier: "controller DOES branch on this — never silently remove."
- **Why it matters**: r7-API2's stated intent was to prevent "silent removal" of `clock.resync-v1` because the controller fails on the call path if missing. The exact same argument applies to `proxy.ws-v1` (`crates/sandbox-agent/src/proxy_ws.rs:27` says the controller feature-detects it) and `auth.ed25519-v1.1` (`sig.rs:187` notes the domain-separator tag is auth.ed25519-v1.1 — controllers that talk v1.1 canonical depend on the membership check). A contributor deprecating one of these without updating the controller side has no regression-pin to trip — the agent test bench would land green, the controller-side breakage shows up in cluster smoke. Each feature-detected cap should have a one-test pin asserting its presence + a comment naming the controller-side dependency, mirroring the `mandatory_clock_resync_v1_present` pattern.
- **Action**: extend `version.rs` with two regression-pin tests (`feature_detected_proxy_ws_v1_present`, `feature_detected_auth_ed25519_v1_1_present`) mirroring `mandatory_clock_resync_v1_present`'s shape — each ~5 LOC including the comment explaining "controller-side feature detection at `<file:line>` depends on this string". OPTIONALLY, refactor `known_capabilities_present` to include the v1.1 set, but a separate per-cap test gives a sharper failure mode when one specifically gets removed.

### [R11-T4] R9-T6 `derive_agent_url` parity test STILL uncovered — and the two formulas are NOW drifting in source (port hardcoded vs named constant) (IMPORTANT, test-coverage-r11, fourth-round carry of R9-T6)

- **Files**: `crates/sandbox/src/restore_handler.rs:1147-1157` (`RealRestoreBackend::derive_agent_url`: `format!("http://10.{}.{}.2:7777", ...)` — port hardcoded as literal `7777`); `crates/sandbox/src/backend/nomad_ch.rs:1530-1536` (`NomadCHBackend::derive_agent_url`: `format!("http://10.{}.{}.2:{AGENT_PORT}", ...)` — uses imported constant); `crates/sandbox-agent/src/lib.rs:41-46` (the AGENT_PORT constant doc-comment explicitly states "Anywhere a backend formats an agent URL (e.g. `http://<vm-ip>:7777`) should reference [`AGENT_PORT`] rather than hard-coding 7777, so a future port change is one edit.").
- **Symptom**: r10 noted this was uncovered. The r11 cycle didn't touch it. Reading the source NOW reveals a stronger problem than r10 reported: the two formulas don't just lack a parity test — they're ALREADY divergent in their dependency on AGENT_PORT. `nomad_ch.rs:1532` correctly uses the named constant (and imports it at line 144). `restore_handler.rs:1152` hardcodes the integer `7777`. If `AGENT_PORT` ever changes (the constant doc says "so a future port change is one edit"), nomad_ch follows but restore_handler does NOT — they silently produce different URLs for the same vm_index, and the controller's two paths (cold-boot vs restore) would dial different ports. This is a code drift that R9-T6's missing parity test was specifically designed to catch — and the source drift has been there since the restore_handler.rs::derive_agent_url was added.
- **Why it matters**: the inline comment at `restore_handler.rs:1148-1151` even says "Same formula `NomadCHBackend::derive_agent_url` uses (lines ~1521 of nomad_ch.rs); centralised here so the RealRestoreBackend doesn't need to thread an Arc<NomadCH> just to compute the URL." But "same formula" already isn't byte-true — one uses a runtime-referenced constant, the other freezes the literal at compile-time. A parity test would catch this drift today. (Note: this is now arguably a code-quality finding too, but it's been in the test-coverage register for four rounds so it stays here.)
- **Action**: add `derive_agent_url_parity_real_backend_vs_nomad_ch` in `crates/sandbox/src/restore_handler.rs::real_backend_tests` mod (~15 LOC). Construct identical configs, call both methods with the same vm_index, assert `RealRestoreBackend::derive_agent_url(vm_index as i16) == NomadCHBackend::derive_agent_url(vm_index as u16)` byte-for-byte across a representative range (e.g. `[0, 5, 100, 255]`). When the test fails, the fix is one edit: replace the literal `7777` in `restore_handler.rs:1153` with `{AGENT_PORT}` (import it at the top of the file). Test-first surfacing of the existing drift.

### [R11-T5] `read_root_owned_secret_file` extract not yet landed — but R9-S4b/c/d closures show the test surface is fragmenting per loader; preempt the post-extract test plan (MINOR, test-coverage-r11)

- **Files**: `crates/sandbox/src/snapshot_aead.rs::RootKek::from_path` (uid + mode + secret-read); `crates/sandbox/src/persist.rs::AeadKey::from_path` (same pattern — closed at e4e5db60 with 2 tests); `crates/sandbox/src/db.rs::enforce_password_file_mode` (same pattern — closed at 2c10f63a with 2 tests); `crates/sandbox/src/lib.rs:924-942` (`load_admin_token` — fourth sibling NOT yet fixed, tracked as R9-S4d in deferred file at line 566).
- **Symptom**: the r11 cycle landed sibling uid fixes at THREE different sites (snapshot_aead.rs, persist.rs, db.rs) — each with its own 2-test pair (uid-check + mode-check). The deferred file at line 566 names a FOURTH site (`load_admin_token`) that will inevitably get its own 2-test pair when fixed. After all four close: EIGHT near-identical tests across four files, each testing the same "0o400 + uid==0" invariant on a different loader. The r11 review's R11-Q2 (per the brief's question 7) flagged that extracting `read_root_owned_secret_file` would consolidate these — but the extract hasn't happened, and each new closure is committing the codebase deeper to the per-loader-test-pair shape.
- **Why it matters**: this is a test-surface-design observation rather than an uncovered behaviour. After the extract:
  1. ONE thorough test of `read_root_owned_secret_file` covers the uid + mode invariants for ALL callers simultaneously. ~10 LOC: feed in (root-owned 0o400), (non-root-owned 0o400), (root-owned 0o600), (root-owned 0o400 + missing-file), (root-owned 0o400 + symlink-to-other), assert each branch.
  2. Per-loader tests shrink to thin "this loader plugs `read_root_owned_secret_file` correctly" assertions (~5 LOC each), pinning the integration shape (correct env-var name, correct error message prefix) WITHOUT re-testing the uid+mode logic.
  3. The R9-S4d follow-up (`load_admin_token`) becomes ~5 LOC instead of ~30 because the uid+mode work isn't duplicated.
- **Action**: not yet actionable as a test-add — the helper doesn't exist. Recommended for the next round of code-quality: before R9-S4d lands, extract `read_root_owned_secret_file` in lib.rs (or a new `secret_files.rs` module). Then the FIVE existing test-pairs (R9-S4, R9-S4b, R9-S4c — three closed; R9-S4d — TBD; plus a future-fifth if someone adds another loader) become the ONE thorough helper test + N thin integration shims. Test count goes DOWN net, coverage goes UP. Flag this BEFORE R9-S4d lands so the closure is in the post-extract shape.

## Closed by recent commits

- **[R7-API2 capability doc-regression]**: closed at c8000537 (+1 test `mandatory_clock_resync_v1_present` in `crates/sandbox-agent/src/version.rs:175-190`). Pins the silent removal of `clock.resync-v1` from the CAPABILITIES list. NOTE: doc rewrite at lines 48-58 named three other "feature-detected" caps without giving them similar pins — see R11-T3.
- **[R10-Q1 err_safe sentinels]**: closed at 228569d3 (+3 sentinel substring tests in `crates/sandbox/src/handlers.rs:1297-1359`). Routes three backend-error sites (`backend_stop_failed`, `backend_exec_failed`, `backend_file_tree_failed`) through `err_safe` to sanitise outbound 500 bodies. Verified by `grep 'err\\(500.*\\{e\\}'` returning empty (per the commit message).
- **[R9-S4b]**: closed at e4e5db60 (+2 tests in `crates/sandbox/src/persist.rs`). `AeadKey::from_path` now refuses non-root-owned files. Sibling of R9-S4.
- **[R9-S4c]**: closed at 2c10f63a (+2 tests in `crates/sandbox/src/db.rs`). `enforce_password_file_mode` now refuses non-root-owned files. Third sibling closed. NOTE: the commit message identified a FOURTH sibling at `crates/sandbox/src/lib.rs::load_admin_token` (~lines 924-942) — verified at HEAD that the mode check is present (line 940) but uid is NOT (no `uid() == 0` check anywhere in the function body). Tracked as R9-S4d in the deferred file.

## Carry-forward (still open from earlier rounds)

- **[R10-T1 / R11-T1 (re-filed)]** `restore_sandbox` end-to-end rollback through R10-C1 + R10-C2 fixes — still uncovered; R10-C2's discarded-JoinHandle at `restore_handler.rs:294` is a new second-order gap. See R11-T1 above.
- **[R10-T2 / R11-T2 (re-filed)]** `Persist(Some(_))` chain at `restore_handler.rs:581-617` — third-round carry; no integration test added. See R11-T2 above.
- **[R10-T3]** `read_sandbox_id_from_sources` cross-source (env-empty + file-present) test in `crates/sandbox-agent/src/handlers.rs:2103-2207`: still 7 tests, none with both env-AND-file set. The "env empty trumps file" semantic edge case remains unpinned.
- **[R10-T4]** `RootKek::from_env` (the production entry called from `AppState::from_config`) under uid invariant: still only `from_path`-driven tests at `crates/sandbox/src/snapshot_aead.rs:1219,1256`. A `from_env`-driven negative arm catches a `#[cfg(not(test))]` short-circuit on the uid check that the current tests would miss.
- **[R10-T5]** `TieredSnapshotStore::put` L2-detach behavioural test: still zero. The fix at `snapshot_store_gcs.rs:1066-1090` wraps L2 in spawn_blocking + `.detach()`, but no test asserts L1 completes before L2 acks.
- **[R9-T3]** `do_restore_inner` spawn_blocking panic-recovery branches — NOW EIGHT sites: SEVEN with explicit panic conversion (`restore_handler.rs:432, 522, 540, 1667`; `snapshot_handler.rs:366, 377, 403`) + the NEW R10-C2 rollback closure at `restore_handler.rs:294` which DISCARDS the JoinHandle via `let _ = ... .await`. Zero tests for any branch; the eighth site has no panic-conversion at all. (Note: r10 noted the discarded-JoinHandle issue in "Notes for the next round" but no follow-up was filed.)
- **[R9-T5]** `build_restore_nomad_job_json` at `restore_handler.rs:1253` — still zero test callers at HEAD. T-7's working-tree changes to `crates/sandbox/src/backend/nomad_ch.rs` (+241/-89, adding `TaskDriverMode::ChPlugin`) will land tests when committed; tracking separately so the count doesn't double-book. The cold-boot sibling at `backend/nomad_ch.rs:3812` (`nomad_job_spec_includes_sandbox_id_env`) is still the only jobspec test.
- **[R9-T6 / R11-T4 (re-filed)]** `derive_agent_url` parity — NOW with confirmed source drift (port-literal vs constant). See R11-T4 above.
- **[R9-T8]** Sweep takeover end-to-end for `Restoring` / `RestoringCold` transient states — only `Snapshotting` exercised at the pg-e2e level; `recovery_target` pure-function still pinned at `sweep.rs:843-860`.
- **[R9-T9]** `/_clock_resync` 4 KiB body-cap 413 test — still no analog of `proxy_http_body_cap_413` (`crates/sandbox-agent/src/proxy.rs:1025`). The cap is implicit via the App-level default `PayloadConfig::default().limit(small_limit=4*1024)` at `main.rs:166`; a per-resource override added later (e.g., to attach a different middleware to `/_clock_resync`) would silently raise the cap.
- **[R9-T10]** `clock_resync_post_restore` controller-side slow-agent + 500-from-agent + malformed-body tests: still 4 tests only at `restore_handler.rs:1984-2046`.
- **[R9-T1 → R10-T2 → R11-T2]** This finding is now in its THIRD CONSECUTIVE ROUND without progress. Recommend the next pilot cycle PIN the integration-test work as a blocker before any new feature commits to the wake path.
- **[r4-T2 / r5-P1b / r7-P1 / r8 / r9 / r10 wake-path latency canary]** still no compio-tick canary in `do_snapshot_inner` or `do_restore_inner`. SIX spawn_blocking wraps in the wake path (per R10's count) now SEVEN (R10-C2 added one); canary sketch applies to all of them.
- **[r3-T3 / r5-T2 / r6 / r7 / r8 / r9 / r10 wrapper script behavioural coverage]** still ZERO behavioural test for `crates/sandbox/scripts/nomad-vm-wrapper.sh`.
- **[T8 ControllerIdleSnapshotter non-pg coverage]** still byte-coverage-zero outside cluster smoke at `crates/sandbox/src/sweep.rs:314-407`.
- **[r6 MEDIUM script artifact consistency tests]** still unwritten.
- **[r8 LOW sig.rs:1475 1-day-future skew-bypass]** still couples nonce-replay assertion to `ts_now() + 86_400`.
- **[R9-P2 (deferred) A2b `verify_metadata_only` zero callers]** still untested at `snapshot_store_gcs.rs:693-718`.

## Notes for the next round

The r11 cycle continued the r10 pattern: all eight new tests added were unit-level in `src/`. Zero integration tests landed (verified by `git diff 040812f2 4f441a20 --stat` over `crates/sandbox/tests` + `crates/sandbox-agent/tests`). The cycle drained four MEDIUM tickets (R9-S4b/c, R10-Q1, R7-API2) — but the two CRITICALs in the test-coverage register (R10-T1 and R10-T2) are now in their third round without progress on the actual integration-through-`restore_sandbox` requirement.

The R10-C2 rollback closure at `restore_handler.rs:294` introduced a new spawn_blocking site that DISCARDS the JoinHandle. This was flagged in r10's "Notes for the next round"; no follow-up landed. The eighth spawn_blocking site (now under R9-T3's count) has no explicit panic-conversion arm — a panic inside `teardown_restore` would silently drop into the discarded `_` without even the "spawn_blocking panic: …" prefix the other seven sites carry. R11-T1 recommends attaching at minimum a tracing::error! to the discard.

The R7-API2 capability doc-regression test (the sandbox-agent's sole +1 this cycle) is an asymmetric pin: it guards `clock.resync-v1` (the mandatory tier) but not `proxy.ws-v1` / `auth.ed25519-v1.1` (the feature-detected tier). The doc rewrite at `version.rs:48-58` elevated the contract by naming three semantic tiers; the test added only protects one. R11-T3 recommends two more one-test pins to close the asymmetry.

R11-T4 surfaces a source-level drift hiding behind the long-running R9-T6 carry: the two `derive_agent_url` formulas don't just lack a parity test, they're already byte-divergent (one uses literal `7777`, the other uses the `AGENT_PORT` constant). A parity test today would FAIL — the test-first surfacing would force the one-line fix at `restore_handler.rs:1153`.

R11-T5 is a test-surface-design preempt: the three closed siblings R9-S4 + R9-S4b + R9-S4c have shipped 6 near-identical tests across three files. R9-S4d (`load_admin_token`) is staged to add 2 more. An extracted `read_root_owned_secret_file` helper would let the next R9-S4d closure land with ~5 LOC of integration shim instead of ~30 LOC of duplicated uid+mode checks — and would consolidate the existing 6 down to ~12 LOC of thorough helper-test + 3×5 LOC integration shims. Flag this BEFORE R9-S4d lands.
