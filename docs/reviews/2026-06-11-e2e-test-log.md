# Framework E2E Test Log — 2026-06-11 (/loop)

Piloted `/loop`: exercise the platform end-to-end — build → deploy → run → every
`env.*` primitive + rendering mode + stage — using existing + newly-created example
apps, capturing every gap. Cross-references the platform gap analysis
(`docs/reviews/2026-06-11-platform-gap-analysis.md`).

Harness: `tests/e2e_platform.sh` (full multi-node stack: control + 3 workers + gateway
on PG :5440), `tests/e2e_db_sqlite.sh`, and per-example `vite-plugin → .zship` builds +
`zeroship serve` smoke. Release binaries built for the platform harness.

## Feature × stage coverage target
Stages: **build** (vite→.zship) · **deploy** (control ingest) · **run** (gateway→worker→V8).
Features: env.db (CRUD/relations/aggregate/tx/mask/encrypt/search/migrate) · env.kv · env.storage ·
env.auth · rpc (query/mutation/action/stream/subscription) · fetch · streams/SSE · websocket ·
node-compat · rendering (CSR/SSR/SSG).

## Gap ledger
(populated each iteration; severity vs the gap analysis)

| # | Stage/feature | Gap | Severity | Evidence |
|---|---|---|---|---|

## Iteration log

### Iter 1 (in progress)
- Kicked off release build of control/gateway/worker/cli.
- Agent A: build + smoke every existing example (build-stage coverage).
- Agent B: feature×stage coverage matrix → what new examples to create.
