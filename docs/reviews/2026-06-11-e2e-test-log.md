# Framework E2E Test Log — 2026-06-11 (/loop)

Piloted `/loop`: exercise the platform end-to-end — build → deploy → run → every
`env.*` primitive + rendering mode + stage — using existing + newly-created example
apps, capturing every gap. Cross-references the platform gap analysis
(`docs/reviews/2026-06-11-platform-gap-analysis.md`).

Harness: `tests/e2e_platform.sh` (full multi-node stack: control + 3 workers + gateway
on PG :5440), `tests/e2e_db_sqlite.sh`, and per-example `vite-plugin → .zship` builds +
`zeroship serve` smoke. Release binaries built for the platform harness.

## Feature × stage coverage target
Stages: **build** (vite→.zship) · **deploy** (control ingest) · **run** (gateway→worker→V8).
Features: env.db (CRUD/relations/aggregate/tx/mask/encrypt/search/migrate) · env.kv · env.storage ·
env.auth · rpc (query/mutation/action/stream/subscription) · fetch · streams/SSE · websocket ·
node-compat · rendering (CSR/SSR/SSG).

## Gap ledger
(populated each iteration; severity vs the gap analysis)

| # | Stage/feature | Gap | Severity | Evidence |
|---|---|---|---|---|
| G1 | run-stage (all primitives) | **No test exercises env.db/kv/storage/auth end-to-end through the multi-node gateway.** `e2e_platform.sh` deploys trivial inline JS (dispatch plumbing only); `db-e2e` runs db richly but via single-tenant `zeroship serve` (bypasses gateway, SQLite not PG). The whole "E2E-thru-gateway" column is empty. | HIGH (test coverage) | Agent B matrix; tests/e2e_platform.sh, examples/db-e2e/e2e/run.mjs |
| G2 | env.storage | **Zero example/test coverage** of a shipped primitive (put/get/delete/list). | HIGH | no examples/* uses @zeroship/storage |
| G3 | env.auth | **Zero example** — no app uses `requireUser()`/`getUser()`; the gateway `ZeroShip-User`→worker AuthPlugin identity chain is never tested end-to-end. | HIGH | db-todos passes userId over the wire instead |
| G4 | env.kv | kv-dashboard exists but has **no test runner** (built, never executed); `setIfAbsent` uncovered. | MED | examples/kv-dashboard (no run.mjs) |
| **G5** | run-stage harness | **`tests/e2e_platform.sh` is BROKEN against current code — the platform's own E2E harness has rotted and silently can't run.** Control refuses to start: WORKER_KEY now required (≥32B) + SIGNING_KEY_FILE required (server-config hardening); the harness sets neither and uses short keys (`test-ck`/`test-mk`). Also stale: `zeroship deploy --key=` (now `--token=`), and an **unqualified `DROP TABLE … apps CASCADE`** (line 108) that, with the `zeroship` search_path, drops `zeroship.apps` + dependents. | **HIGH** | ran the harness; control logs "WORKER_KEY is required", then "SIGNING_KEY_FILE missing"; starts only with `--dev-insecure`. |
| — | dev-DB damage (self-inflicted, to repair) | Running the stale harness's unqualified `DROP … apps` removed `zeroship.apps` from the shared :5440 dev DB. Plus pre-existing Liquibase checksum drift at 0034 (manual DDL during auth work). The :5440 DB is not a clean schema reflection. | infra | `SELECT FROM zeroship.apps` → "relation does not exist" |

**Next-iteration setup (blocking faithful run-stage E2E):** a clean, fully-migrated PG (Liquibase 0001→0036 from scratch) + a modernized harness (`--dev-insecure` or real keys+signing-key, `--token` not `--key`, qualified/temp-DB cleanup). Use a CLEAN EPHEMERAL PG for E2E (don't further touch shared :5440); separately repair :5440's dropped `apps`.

**Known-absent (capability gaps, not coverage gaps — do NOT build examples):** rpc subscription/WebSocket-thru-gateway (501 by design), env.meter, env.assets, env.authz/permissions, @zeroship/{email,ai}, non-additive migrations, S3/custom-domains/rollback/cron, node:{fs,http,child_process,module,timers,url} (build↔runtime mismatch). Cross-ref `2026-06-11-platform-gap-analysis.md`.

**Iteration-2 plan (once binaries + Agent A land):** create `storage-gallery` (env.storage) + `auth-notes` (env.auth, exercises the gateway→worker identity chain) + a kv test runner; and — highest leverage — a gateway-E2E harness (`e2e_app_primitives.sh`) that deploys a real built example through control→gateway→worker and asserts the primitives work over the real edge (closes G1).

## Iteration log

### Iter 1 (in progress)
- Kicked off release build of control/gateway/worker/cli.
- Agent A: build + smoke every existing example (build-stage coverage).
- Agent B: feature×stage coverage matrix → what new examples to create.
