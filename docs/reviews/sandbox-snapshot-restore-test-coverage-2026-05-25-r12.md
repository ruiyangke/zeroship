# Sandbox/snapshot-restore — test-coverage r12 review

Date: 2026-05-25 (UTC)
HEAD at audit: 9f1dfc99
Round 12 of N (test-coverage lens).

## Summary

- 4 NEW findings (1 critical, 2 important, 1 minor) + 2 carry-forwards re-filed
- Test counts at HEAD (verified by cargo run): sandbox lib **327 passed / 1 ignored / 0 failed** (was 319 at r12 cycle start, 310 at r11); sandbox-agent lib **242 passed / 0 ignored / 0 failed** (was 240 at r11). Aggregate cycle delta: **+17 sandbox lib, +2 sandbox-agent = +19**.
- The cycle's +19 split: R12-I1 wake-path (+5 in `restore_handler.rs::r12_i1_tests`), R11-S2 host_id (+3 in `db.rs`), R9-S4d admin token uid (+2 in `lib.rs`), R11-T3 capability pins (+2 in `sandbox-agent/version.rs`), R11-P2/P3 GCS BufReader/BufWriter (+5 across `snapshot_store_gcs.rs`), with the residual +2 from earlier intra-cycle micro-adds.
- Three R-tickets closed this cycle: **R11-T3** (capability pins, eb26db31), **R11-S2** (host_id uid+mode, 85e4f2f9), **R11-P2 + R11-P3** (GCS BufReader/BufWriter + SHA helpers, 3d5c527f). R9-S4d (admin token uid) ALSO closed at b4c3ef27. R12-I1 closed at b3bf741c.
- The two THIRD-ROUND CRITICAL items in the test-coverage register (R10-T1 / R11-T1 and R10-T2 / R11-T2) remain OPEN — they enter their **fourth consecutive round** without an end-to-end integration test driving `restore_sandbox` through the rollback path or the `Persist(Some(_))` chain. R12-I1's +5 tests are builder-shape tests on `build_restore_nomad_job_json`, not integration tests through `restore_sandbox`.
- The R10-A4 module-split push (deferred file line 697) has not landed; R12-I1's choice of "approach (b) — match-on-mode in a separate restore builder" deferred the merge (per its commit message), so the duplication-shape that R10-A4 targets stays open and `nomad_ch.rs` is still **5371 LOC** at HEAD with **36 tests** under a single `mod tests` block at line 3421.

## Trend table (5-cycle test count history)

| Cycle | sandbox lib | sandbox-agent lib | Δ sandbox | Δ sandbox-agent | Notes |
|-------|-------------|-------------------|-----------|-----------------|-------|
| r8    | (pre-r9)    | (pre-r9)          | —         | —               |       |
| r9    | 296         | 231               | —         | —               | baseline |
| r10   | 303         | 239               | +7        | +8              | R7-S1, R8-A2, B23, B24 +6 R-closes |
| r11   | 310         | 240               | +7        | +1              | R7-API2 + R10-Q1 + R9-S4b/c |
| r12 (start) | 319   | 240               | +9        | 0               | R11-T3 + R9-S4d + R11-P2/P3 + R11-S2 |
| r12 (HEAD)  | 327   | 242               | +8        | +2              | R12-I1 (+5) + r11 carry-finishers |
| **r9 → r12 total** | **+31** | **+11**  | —         | —               | 5-round growth |

The two-cycle growth of 17 (r10→r11) → 19 (r11→r12) is **roughly flat** — the cadence is sustainable but the SHAPE remains: every test added this cycle was UNIT-LEVEL in `src/`. Integration test files (`crates/sandbox/tests/`, `crates/sandbox-agent/tests/`) saw **zero new tests this cycle** (verified by `git diff 4f441a20 9f1dfc99 -- crates/sandbox/tests crates/sandbox-agent/tests` returning empty for both). This is the third consecutive cycle with that pattern — every R-close has been wide and shallow, never deep through `restore_sandbox`.

## Findings (NEW since r11)

### [R12-T1] R12-I1's +5 tests are pure builder-shape — `restore_sandbox` end-to-end rollback STILL untested; R10-T1 / R11-T1 / R11-T2 now FOURTH-round criticals (CRITICAL, test-coverage-r12, fourth-round carry of R10-T1 + R11-T1 + R11-T2)

- **Files**: `crates/sandbox/src/restore_handler.rs:2450-2674` (the new `mod r12_i1_tests`, 5 tests); `crates/sandbox/src/restore_handler.rs:1276-1467` (`build_restore_nomad_job_json` — the only function the 5 tests drive); `crates/sandbox/src/restore_handler.rs:261-313` (rollback closure with the still-discarded JoinHandle at line 294); `crates/sandbox/src/restore_handler.rs:728-743` (`StubRestoreBackend::fail_submit` + `fail_livez` — declared but zero pg-test sets them); `crates/sandbox/tests/sandbox_pg_e2e.rs:2707,2757,2785,3237,3359` (every `restore_sandbox` callsite at HEAD; verified by grep — only line 2781's `fail_reserve = true` is the failure path tested, which trips the early `VmIndexUnavailable` arm BEFORE the rollback closure becomes reachable).
- **Symptom**: r10 → r11 → r12. Three consecutive rounds have asked for "one pg-gated integration test that wires `StubRestoreBackend { fail_submit: true, .. }` through `restore_sandbox` and asserts (a) `teardown_called=true`, (b) row reaches `Snapshotted` (rollback CAS landed), (c) released vector contains the vm_index". R12 cycle's +5 sandbox-lib tests are **builder-shape only**: each one constructs a `NomadCHConfig` fixture, calls `build_restore_nomad_job_json(...)` directly, and asserts on the returned `serde_json::Value` shape. They never construct a `Database`, never call `restore_sandbox`, never reach the rollback closure at line 294, never observe the R10-C2 spawn_blocking wrap under failure. The R10-C1 tests at lines 2264-2394 DO use the `RealRestoreBackend` (and even drive `teardown_restore` directly) but invoke it through `backend.teardown_restore(id, vm_index)` — bypassing `do_restore_inner` and its rollback closure. The R10-C2 test at line 2405 is structural (`include_str!` text-grep on the source). After 4 rounds, the integration shape of "rollback fires through `restore_sandbox`, vm_index leak detected, status row CAS verified" is still uncovered by any test in the workspace.
- **Why it matters**: the `restore_sandbox` rollback closure is the single highest-value behavioural surface this PR set adds — R10-C1 + R10-C2 (state-map cleanliness + worker-park immunity) are pinned ONLY at the callee level. A regression that, e.g., removes the spawn_blocking wrap AND moves the anchor comment in the same commit, the R10-C2 grep at line 2417 fails saying "R10-C2 anchor comment moved; update the test" — i.e., the contributor is told to fix the TEST, not the CODE. R11-T1's "8th spawn_blocking site with `let _ = …await` discard" (line 294) is now confirmed by R12-M1 in the deferred file (line 967) — a panic inside `teardown_restore` becomes a silently-swallowed JoinError. A behavioural test that hooks the discard path through `StubRestoreBackend` would surface the swallowing AND pin the rollback invariants. NOTE: between r11 and r12, the +19 cycle drained four MEDIUM tickets (R11-T3, R11-S2, R11-P2/P3, R9-S4d) plus the R12-I1 critical, but the critical that has been on the test-coverage register longest got the LEAST attention — its dependency surface (`StubRestoreBackend::fail_submit` / `fail_livez`) was DECLARED to support exactly this work but stayed unused for the third cycle running.
- **Action**: unchanged from R10-T1 / R11-T1. ONE pg-e2e integration test (`restore_sandbox_rollback_after_submit_failure_via_teardown_restore`) constructing `StubRestoreBackend { fail_submit: true, .. }`, calling `restore_sandbox(&db, store, backend, None, sid, true).await`, asserting (a) row.status == `Snapshotted` (rollback CAS landed); (b) `backend.teardown_called.load(SeqCst) == true`; (c) `backend.released.lock().contains(&vm_index)`; (d) artifact still on disk. Companion test for `fail_livez=true`. Both ~40 LOC; mirror the existing pattern at `tests/sandbox_pg_e2e.rs:2780-2790` but flip `fail_reserve` → `fail_submit`. SEPARATELY, fix line 294's `let _ = …await` discard by adding `.unwrap_or_else(|p| tracing::error!("teardown spawn_blocking panic: {p:?}"))` to match the other 3 sites' pattern (R12-M1's stated action, 4-line edit).

### [R12-T2] `Persist(Some(_))` AEAD + clock_resync + register_restored chain — FOURTH-round carry, zero new coverage (CRITICAL, test-coverage-r12, fourth-round carry of R9-T1 / R10-T2 / R11-T2)

- **Files**: `crates/sandbox/src/restore_handler.rs:581-617` (the `if let Some(p) = persist` chain — unseal + `clock_resync_post_restore` + `register_restored`); `crates/sandbox/tests/sandbox_pg_e2e.rs:2707,2757,2785,3237,3359` (every callsite STILL passes `None` for persist — verified at HEAD by grep `git show 9f1dfc99:crates/sandbox/tests/sandbox_pg_e2e.rs | grep -n "persist:\|AeadSnapshotStore\|Persistence::"` returning empty matches for the wake-path persist=Some shape).
- **Symptom**: this finding's first surfacing was R9-T1, re-filed as R10-T2, re-filed as R11-T2, re-filed here as R12-T2. **Four consecutive rounds without progress.** R12 cycle's +19 tests added zero coverage of the wake-path persist chain. R11-P2 + R11-P3's +5 GCS tests are in `snapshot_store_gcs.rs` (BufReader/BufWriter wrappers + SHA helpers), R11-S2's +3 are in `db.rs` (host_id reader hardening), R9-S4d's +2 are in `lib.rs` (admin_token uid), R11-T3's +2 are in `sandbox-agent/version.rs` (capability pins), R12-I1's +5 are builder-shape on a NEW function. None drives `restore_sandbox` with `persist=Some(...)`.
- **Why it matters**: the chain at lines 581-617 is the entire B19 + B22 fix surface — `register_restored` (state-map install) + `clock_resync_post_restore` (post-wake 401 / clock-skew). A future regression that swaps clock_resync and register_restored ordering, or that drops the seal-roundtrip on the signing key, would land green because every pg-test path hits the `None` branch at lines 607-617. The R9-P1 deferred AEAD-wake-regression (3 I/O passes on memory-ranges) remains uncovered because no pg-test runs wake against an AEAD-wrapped store. Compounded with R12-T1: BOTH critical integration carries — the rollback chain AND the persist-chain — share the same "every pg-test goes down the None branch" symptom and would benefit from a SINGLE infrastructure investment (a fake-agent harness + `AeadSnapshotStore::new(...)` fixture).
- **Action**: unchanged from R10-T2 / R11-T2. ONE pg-gated test wiring `restore_sandbox` with (a) `AeadSnapshotStore::new(LocalDiskSnapshotStore, Some(kek))` for `store`, (b) a real `Persistence` (seal a fake signing key), (c) a fake-agent endpoint that the controller's `clock_resync_post_restore` can POST to. Assert: (i) row reaches Running; (ii) register_restored invoked exactly once with the sealed key; (iii) on-disk artifact ciphertext starts with `ZSBXAEAD` magic; (iv) fake agent recorded exactly one POST to `/_clock_resync`. Same shape as snapshot↔restore round-trip at `tests/sandbox_pg_e2e.rs:3001-3119`, but with `persist=Some(...)` instead of `None`.

### [R12-T3] Wake-path under `ChPlugin` mode passes builder-shape pins but `submit_restore_job` integration ENTIRELY untested (IMPORTANT, test-coverage-r12)

- **Files**: `crates/sandbox/src/restore_handler.rs:1101-1145` (`submit_restore_job` — the production caller that reads `task_driver_mode_from_env()` and threads through to `build_restore_nomad_job_json`); `crates/sandbox/src/restore_handler.rs:2450-2674` (R12-I1 tests — 5 tests, ALL of them call `build_restore_nomad_job_json` directly with a synthesised mode arg); `crates/sandbox/src/restore_handler.rs::real_backend_tests` at line 1775 (the existing `RealRestoreBackend` test mod — has fake-nomad spawn helpers `spawn_fake_nomad` at line 1814 BUT no test exercises the env→mode→jobspec→POST chain).
- **Symptom**: R12-I1 (the cycle's headline closure, T-8 blocker) added 5 tests that pin the *builder's* behaviour under both modes. But the production integration shape — `submit_restore_job` reads `task_driver_mode_from_env()` once, threads it to `build_restore_nomad_job_json`, serializes the JSON, POSTs to `/v1/jobs` — has zero coverage. A regression at `submit_restore_job` line 1116 that, e.g., hardcodes `TaskDriverMode::RawExec` instead of reading the env, would land green because every R12-I1 test passes `mode` explicitly as a parameter. The existing `spawn_fake_nomad` test harness at line 1814 (used by R10-C1) demonstrates the pattern is achievable: spawn a fake Nomad on 127.0.0.1, call `submit_restore_job`, capture the POST body, assert `body.Job.TaskGroups[0].Tasks[0].Driver == "ch"`.
- **Why it matters**: the T-8 cutover risk R12-I1 was filed to prevent — split-brain CREATEs-via-ch / RESTOREs-via-raw_exec — depends on the env→mode chain working in production. The builder tests don't catch a callsite regression where the env-read drops out. Pairs with R12-M2 in deferred (cache TaskDriverMode at NomadCHBackend construction); a callsite regression test would also exercise the read-once-at-startup pattern when R12-M2 lands.
- **Action**: add `submit_restore_job_under_ch_plugin_env_emits_ch_driver_jobspec` in `real_backend_tests` mod (~50 LOC). Set `SANDBOX_TASK_DRIVER=ch_plugin` under the same module-local `R12_I1_ENV_LOCK` lock, spawn `spawn_fake_nomad` capturing the POST body, call `backend.submit_restore_job(sid, vm_index, &alloc_dir, "usr_alice")`, parse the captured body as JSON, assert `task["Driver"] == "ch"`. Plus the inverse `submit_restore_job_default_emits_raw_exec_jobspec`. Catches both R12-I1's callsite contract AND the future R12-M2 caching refactor's correctness.

### [R12-T4] R9-T6 / R11-T4 — derive_agent_url drift is now THREE hardcoded sites (was TWO in r11); parity test STILL uncovered (IMPORTANT, test-coverage-r12, FIFTH-round carry of R9-T6)

- **Files**: `crates/sandbox/src/restore_handler.rs:1162` (`RealRestoreBackend::derive_agent_url` — `format!("http://10.{}.{}.2:7777", ...)`, literal `7777`); `crates/sandbox/src/restore_handler.rs:1241` (`RealRestoreBackend::register_restored` — `format!("http://10.{}.{}.2:7777", ...)`, literal `7777`); `crates/sandbox/src/backend/nomad_ch.rs:1532` (`NomadCHBackend::derive_agent_url` — `format!("http://10.{}.{}.2:{AGENT_PORT}", ...)`, named const); `crates/sandbox/src/backend/nomad_ch.rs:792` (also `AGENT_PORT`); `crates/sandbox-agent/src/lib.rs:41-46` (the AGENT_PORT doc explicitly states "Anywhere a backend formats an agent URL (e.g. `http://<vm-ip>:7777`) should reference [`AGENT_PORT`] rather than hard-coding 7777, so a future port change is one edit.").
- **Symptom**: r11 reported TWO sites diverging (one literal, one named const). At r12 HEAD the divergence is now THREE-site: `register_restored` at line 1241 (added when B19 fix landed, also hardcodes `7777`). r11's drift observation has gotten WORSE, not better. The same `RealRestoreBackend` struct now has TWO copies of the literal `7777` (lines 1162, 1241) AND the inline comment at lines 1238-1239 says "Same agent_url formula as `wait_for_livez` above" — i.e., the author noted the duplication but chose copy-paste over factoring. If `AGENT_PORT` ever changes, `nomad_ch.rs` follows, but both `restore_handler.rs` callsites DON'T. The R10-A4 module-split push (deferred line 697) would, if executed by the proposed `nomad_ch/{create,stop,restore,jobspec,...}.rs` plan, scatter the `restore` URL-formatter into a separate file from `create`, making the drift permanent and structural.
- **Why it matters**: the controller's restore path dials `register_restored`'s URL to install the state-map entry AND `derive_agent_url`'s URL to wait for livez. A regression where the literal `7777` drifts from `AGENT_PORT` would cause restore to dial the WRONG port silently (200 from livez might come from a different service if `AGENT_PORT` is, say, 17777 in some future config), and the state-map entry's `agent_url` field would be stale forever after. Five rounds is a long time for a one-line fix.
- **Action**: ONE parity test in `real_backend_tests` mod: `derive_agent_url_parity_real_backend_vs_nomad_ch` (~20 LOC). Construct identical configs, call `RealRestoreBackend::derive_agent_url(idx as i16)` AND `NomadCHBackend::derive_agent_url(idx as u16)` for `idx ∈ {0, 5, 100, 255}`, assert byte-for-byte equality. ADDITIONAL: `register_restored_agent_url_matches_derive_agent_url` to pin the intra-`RealRestoreBackend` drift between lines 1162 and 1241. When the test fails, the fix is two edits (replace literal `7777` with `{AGENT_PORT}` at lines 1162 + 1241, import `AGENT_PORT` at the top of the file). Test-first surfacing of the existing drift.

### [R12-T5] R10-A4 module-split prep: every nomad_ch.rs test (36 total) lives in ONE flat `mod tests`; post-split orphan risk (MINOR, test-coverage-r12)

- **Files**: `crates/sandbox/src/backend/nomad_ch.rs:3421-3422` (single `#[cfg(test)] mod tests {` at line 3422 containing ALL 36 tests, spanning ~1950 LOC down to line 5371); the deferred file R10-A4 (line 697) proposes the split `nomad_ch/{create,stop,restore,jobspec,allocator,state,ch_remote,tests}.rs`; `crates/sandbox/src/restore_handler.rs:2450` (the NEW `mod r12_i1_tests` — landed this cycle in `restore_handler.rs` even though its target `build_restore_nomad_job_json` IS in `restore_handler.rs`, but the parallel-shape `nomad_job_spec_uses_raw_exec_by_default` lives at `nomad_ch.rs:4099` under the `tests` mod — the family is now fragmented across TWO files).
- **Symptom**: with R10-A4 still pending, the 36 tests in `nomad_ch.rs::tests` are pre-categorised by their containing fixture functions but not by submodule. Post-split, the mapping would naturally be: `vm_alloc_*` (7 tests) → `allocator.rs`; `allocs_terminal_*` (5 tests) → `state.rs` or `ch_remote.rs`; `nomad_job_*` + `ch_plugin_*` + `raw_exec_*` (10 tests) → `jobspec.rs`; `state_map_entry_*` (1 test) → `state.rs`; `cpus_boot_*`/`nomad_cpu_*` (3 tests) → `jobspec.rs`; `workspace_image_*`/`user_home_image_*`/`create_ext4_*` (3 tests) → `create.rs`; `sanitize_*`/`validate_typed_id_*` (6 tests) → likely `jobspec.rs` or a `validation.rs` helper; and ~5 mock-agent + handler tests. The R12-I1 family (5 tests) currently sits at `restore_handler.rs:2450-2674` — post-split, the cold-boot jobspec tests would move to `nomad_ch/jobspec.rs` while R12-I1's tests stay in `restore_handler.rs::r12_i1_tests`, separating sibling builder tests across two files even though they're testing the SAME `TaskDriverMode` enum surface.
- **Why it matters**: this is a test-surface-design preempt rather than a coverage gap. After R10-A4 lands, the natural action is to migrate the 36 `nomad_ch::tests` tests file-by-file with each split commit (one commit per child module). If R12-I1's tests stay in `restore_handler.rs` they'll be ORPHANED from their siblings — a contributor adding a new TaskDriverMode mode (T-9?) would have to update `nomad_ch/jobspec.rs::tests` AND `restore_handler.rs::r12_i1_tests` to keep the cold-boot and wake-path coverage parallel. Better outcome: move R12-I1's 5 tests into `nomad_ch/jobspec.rs::tests` (or wherever the matching `nomad_job_spec_uses_raw_exec_by_default` etc. live post-split). Even-better outcome: factor `build_restore_nomad_job_json` itself into `nomad_ch/jobspec.rs` since it shares 80% of its body with `build_nomad_job_json_with`, addressing R12-I1's commit-message "approach (a) merge deferred" remark.
- **Action**: BEFORE R10-A4 lands, decide where R12-I1's tests will end up. The cleanest answer is to move both `build_restore_nomad_job_json` AND `mod r12_i1_tests` into `nomad_ch::jobspec` (the future child module) — the function is fundamentally a Nomad-jobspec builder, not a restore-handler concern; it lives in `restore_handler.rs` today only because that's where it was first added. When R10-A4 PR sequence runs, the child split for `jobspec.rs` should pull in both `build_nomad_job_json_with` AND `build_restore_nomad_job_json` as siblings. No new tests required; the action is to flag the move-target before R10-A4's first split commit lands. Tracking-only finding.

## Closed by recent commits (since r11 audit at 4f441a20)

- **[R11-T3 capability pins]**: CLOSED at `eb26db31` (+2 tests `feature_detected_proxy_ws_v1_present` + `feature_detected_auth_ed25519_v1_1_present` in `crates/sandbox-agent/src/version.rs`). Pins the silent removal of `proxy.ws-v1` and `auth.ed25519-v1.1` from the CAPABILITIES list, mirroring the r11 cycle's `mandatory_clock_resync_v1_present`. Both tiers (mandatory + feature-detected) now have presence-pin tests; the doc rewrite's three-tier contract is fully test-anchored.
- **[R11-S2 host_id uid+mode]**: CLOSED at `85e4f2f9` (+3 tests in `crates/sandbox/src/db.rs`). `read_host_id` now enforces mode 0o600 + uid==0. Bonus: previous path silently regenerated on any read failure (silent-fail-OPEN footgun); new path surfaces permission errors as `Validation`.
- **[R9-S4d admin token uid]**: CLOSED at `b4c3ef27` (+2 tests in `crates/sandbox/src/lib.rs`). `load_admin_token` now refuses non-root-owned files. Fourth and final sibling in the R9-S4 / R9-S4b / R9-S4c family; the predicted "8 near-identical tests across four files" shape (per r11's R11-T5) is now reality. R11-T5's recommendation to extract `read_root_owned_secret_file` BEFORE this closure was not adopted; the helper-extract is still possible as a follow-up but now requires consolidating 4 existing test-pairs rather than 3.
- **[R11-P2 + R11-P3 GCS BufReader/BufWriter + SHA helpers]**: CLOSED at `3d5c527f` (+5 tests in `snapshot_store_gcs.rs`). Wraps GCS download/upload streams; adds SHA helpers. (Performance category — out of test-coverage scope but counted in the cycle's +19 total.)
- **[R12-I1 wake-path SANDBOX_TASK_DRIVER]**: CLOSED at `b3bf741c` (+5 tests in `restore_handler.rs::r12_i1_tests`). T-8b-cutover unblocker. NOTE: the closure is at the BUILDER level only — see R12-T1 + R12-T3 above for the integration-shape gaps that remain.

### NOT closed by R12-I1 (3rd-cycle / 4th-cycle assessment per the brief)

- **[R10-T1 / R11-T1]**: NOT closed. R12-I1's +5 tests drive `build_restore_nomad_job_json` directly, NOT `restore_sandbox`. The rollback closure at `restore_handler.rs:294` still has zero behavioural test; `StubRestoreBackend::fail_submit` / `fail_livez` flags still have zero pg-test that sets them. Re-files as **R12-T1** (4th-round CRITICAL).
- **[R10-T2 / R11-T2]**: NOT closed. R12-I1's tests don't touch `Persist(Some(_))`. The wake-path persist chain at `restore_handler.rs:581-617` is still 100% uncovered by any pg-test. Re-files as **R12-T2** (4th-round CRITICAL).

## Carry-forward (still open from earlier rounds)

- **[R10-T1 / R11-T1 / R12-T1 (re-filed, 4th round)]** `restore_sandbox` end-to-end rollback through R10-C1 + R10-C2 fixes — still uncovered. See R12-T1 above.
- **[R10-T2 / R11-T2 / R12-T2 (re-filed, 4th round)]** `Persist(Some(_))` chain at `restore_handler.rs:581-617` — still uncovered. See R12-T2 above.
- **[R10-T3]** `read_sandbox_id_from_sources` cross-source (env-empty + file-present) test in `crates/sandbox-agent/src/handlers.rs`: still 7 tests, none with both env-AND-file set. The "env empty trumps file" semantic edge case remains unpinned. (3rd-round carry.)
- **[R10-T4]** `RootKek::from_env` (the production entry called from `AppState::from_config`) under uid invariant: still only `from_path`-driven tests. (3rd-round carry.)
- **[R10-T5]** `TieredSnapshotStore::put` L2-detach behavioural test: still zero. (3rd-round carry.)
- **[R9-T3]** `do_restore_inner` spawn_blocking panic-recovery branches — STILL EIGHT sites: SEVEN with explicit panic conversion + the R10-C2 rollback closure at `restore_handler.rs:294` discarding the JoinHandle via `let _ = ... .await`. R12-M1 in the deferred file (line 967) now ALSO flags this independently. Zero tests for any branch. (4th-round carry.)
- **[R9-T5]** `build_restore_nomad_job_json` test callers at HEAD: +5 from R12-I1 (closes part of the carry, but only the builder shape). The cold-boot sibling `nomad_job_spec_includes_sandbox_id_env` at `backend/nomad_ch.rs:3974` is no longer the lone jobspec test. **PARTIAL CLOSURE**: builder-level coverage landed (R12-I1); integration via `submit_restore_job` remains uncovered (R12-T3).
- **[R9-T6 / R11-T4 / R12-T4 (re-filed, 5th round)]** `derive_agent_url` parity — drift now THREE hardcoded sites (was 2 in r11). See R12-T4 above.
- **[R9-T8]** Sweep takeover end-to-end for `Restoring` / `RestoringCold` transient states — only `Snapshotting` exercised at the pg-e2e level. (4th-round carry.)
- **[R9-T9]** `/_clock_resync` 4 KiB body-cap 413 test — still no analog of `proxy_http_body_cap_413`. (4th-round carry.)
- **[R9-T10]** `clock_resync_post_restore` controller-side slow-agent + 500-from-agent + malformed-body tests: still 4 tests only at `restore_handler.rs:1984-2046`. (4th-round carry.)
- **[r4-T2 / r5-P1b / r7-P1 / r8 / r9 / r10 / r11 wake-path latency canary]** still no compio-tick canary in `do_snapshot_inner` or `do_restore_inner`. R10-C2 added a 7th spawn_blocking wrap; r12 unchanged. (8th-round carry.)
- **[r3-T3 / r5-T2 / r6 / r7 / r8 / r9 / r10 / r11 wrapper script behavioural coverage]** still ZERO behavioural test for `crates/sandbox/scripts/nomad-vm-wrapper.sh`. (9th-round carry.)
- **[T8 ControllerIdleSnapshotter non-pg coverage]** still byte-coverage-zero outside cluster smoke at `crates/sandbox/src/sweep.rs:314-407`. (3rd-round carry.)
- **[r6 MEDIUM script artifact consistency tests]** still unwritten. (6th-round carry.)
- **[r8 LOW sig.rs:1475 1-day-future skew-bypass]** still couples nonce-replay assertion to `ts_now() + 86_400`. (4th-round carry.)
- **[R9-P2 (deferred) A2b `verify_metadata_only` zero callers]** still untested at `snapshot_store_gcs.rs`. (4th-round carry.)
- **[R11-T5 — read_root_owned_secret_file extract]** R9-S4d (the 4th sibling) closed at b4c3ef27 with the per-loader test-pair shape, NOT the consolidated helper-extract shape r11 recommended. Test count is now 4 × 2 = 8 near-identical tests across four files (snapshot_aead.rs, persist.rs, db.rs, lib.rs). Carry-forward: post-extract, these 8 tests collapse to ~1 thorough helper test + 4 thin integration shims. Code-quality finding, not test-coverage, but tracked here for closure intent.

## Notes for the next round

The r12 cycle's defining pattern is that the **CRITICAL test-coverage gap on `restore_sandbox` integration** entered its **fourth consecutive round** without an end-to-end test, despite the cycle landing 19 new tests, FIVE R-closures including a T-8 blocker, and a +5 test family that explicitly addressed the wake-path codepath that R10-T1 was filed against. The R12-I1 commit message even notes "approach (b) over (a)" with the rationale that the wake builder has substantive divergence from the cold-boot builder — accepting code duplication today in exchange for a narrower diff. That trade-off is reasonable in isolation, but the test surface it leaves behind is *only* the builder shape. The actual production contract — `submit_restore_job` reads the env, threads it to the builder, POSTs to Nomad, handles failure — has the same surface it had at r9: untested through `restore_sandbox`.

R12-T3 (new this round) recommends `submit_restore_job_under_ch_plugin_env_emits_ch_driver_jobspec` as a callsite-integration test using the existing `spawn_fake_nomad` harness from R10-C1. This is the smallest possible step toward the larger R10-T1 mandate: it doesn't require a full `Database` fixture, just a fake Nomad and an env mutex. The harness already exists in `real_backend_tests` (line 1814). If R12-I1's tests had landed in `real_backend_tests` rather than a separate `r12_i1_tests` mod, this callsite test would have been a natural follow-on; instead the cycle landed them in isolation and the integration shape stayed uncovered.

R12-T4 (re-filed R9-T6 / R11-T4) deserves an explicit note: this is the **fifth** consecutive round flagging the same `derive_agent_url` parity gap, and the source has gotten WORSE between r11 and r12 — the `register_restored` site at line 1241 added a SECOND hardcoded `7777` inside the same struct that already had one at line 1162. The fix is two `format!` edits + one `use` import + one 20-LOC test. The cumulative cycle cost of carrying this forward five times far exceeds the one-time fix cost. Recommend pinning it as a blocker on the next pilot cycle.

R12-T5 is a tracking-only preempt for the upcoming R10-A4 module split. The 5 R12-I1 tests landed in a new `mod r12_i1_tests` inside `restore_handler.rs` even though their target is a Nomad-jobspec builder that naturally belongs under `nomad_ch/jobspec.rs` post-split. Flagging this BEFORE R10-A4's first split commit lands so the migration plan can move the function + its tests together.

The R11-T5 helper-extract recommendation (consolidate `read_root_owned_secret_file` across the 4 sibling loaders) was NOT adopted before R9-S4d closed — so the codebase now has 8 near-identical tests pinning the same uid+mode invariant. This is now an explicit test-surface-debt entry: consolidating later requires touching 4 files. Not blocking, but the next code-quality cycle could shrink the test surface materially.
