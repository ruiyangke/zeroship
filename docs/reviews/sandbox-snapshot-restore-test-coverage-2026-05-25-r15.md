# Sandbox/snapshot-restore — test-coverage r15 review

Date: 2026-05-25 (UTC)
HEAD at audit: `b8fae7b7` (per task brief — committed only; worktree carries uncommitted R14-A6 WIP on `restore_handler.rs` (+234/-21) which is OUT of audit scope).
Round 15 of N (test-coverage lens).
Prior round: `docs/reviews/sandbox-snapshot-restore-test-coverage-2026-05-25-r14.md` (at `1482725d`).

## Summary

- 2 NEW findings (1 CRITICAL elevating to **EMERGENCY**, 1 IMPORTANT) + 6+ carry-forwards re-filed.
- Test counts at HEAD `b8fae7b7` by `cargo test`: sandbox lib **332 passed / 1 ignored / 0 failed** (unchanged from r14 HEAD `1482725d`); sandbox-agent lib **242 passed / 0 ignored / 0 failed** (unchanged from r14). **Aggregate cycle delta: 0 sandbox lib (one test replaced — `c4_default_policy_envelopes_observed_teardown` deleted, `c7_retry_budget_default_is_under_client_deadline` added; net 0), 0 sandbox-agent. CYCLE NET-NEW: 0 TESTS.**
- The 7-round CRITICAL carry (R10-T1 / R11-T1 / R12-T1 / R13-T2 / R14-T1 — `restore_sandbox` end-to-end rollback through `StubRestoreBackend::fail_submit` / `fail_livez` + the `Persist(Some(_))` chain) is now in its **7th consecutive round**. Architecture r13 → r14 R13-A1 / R14-A* lineage already CRITICAL with cross-lens consensus. Smoke-r7 + smoke-r8 (`docs/reviews/sandbox-snapshot-restore-cluster-2026-05-25-T8b-smoke-r7.md`, `r8.md`) are TWO MORE empirical confirmations that the unit-test surface cannot detect cluster-shape failure modes: **r7 found C-6 (compio runtime starvation by detached teardown), r8 falsified that hypothesis and pinned C-7 (client-disconnect future-drop)**. Each is a new failure-mode the helper unit tests structurally cannot reach. The 1-cycle-1-new-bug pattern is now **7 cycles deep, 7 distinct bugs (C-1 ... C-7)**.
- The C-6 fix at `91ce9be5` landed **+0 / -0 tests** (verified in commit message + `git show 91ce9be5 -- crates/sandbox/src/`). The C-7 fix at `493d6c1e` landed **+1 / -1 tests** (one replaced, net 0). The structural integration gap that produced C-1...C-7 has had **ZERO** new pg-e2e fixtures in 7 consecutive cluster cycles.
- The C-4 #4 `c4_default_policy_envelopes_observed_teardown` test (the SUBJECT of r14-T2 IMPORTANT) was deleted by `493d6c1e` because its `>=90_000ms` assertion DIRECTLY CONTRADICTED the new C-7 invariant (`+5s headroom <= 60_000ms`). This is the first time a `c4_*` family test has been deleted; the r14 finding predicted brittleness and the deletion is the empirical receipt. The replacement `c7_retry_budget_default_is_under_client_deadline` (lines 1414-1453) repeats the SAME constant-arithmetic-only pattern that r14-T2 flagged — flipped inequality direction, but no behavioural drive of the policy through the loop. **The defect class r14-T2 named is now itself re-filed as r15-T2 with one cycle's worth of additional receipts.**
- The C-6 OS-thread detach pattern at `admin_handlers.rs:1340-1393` (introduced `91ce9be5`) is **byte-coverage-zero** — no test exercises `std::thread::Builder::spawn` + `compio::runtime::Runtime::new().block_on(...)` for the teardown path. The commit message explicitly states "the bug requires a dual-task compio scenario that the current test harness doesn't drive; cluster smoke-r8 is the validation gate." This is the second cycle in a row a C-N regression fix EXPLICITLY relies on cluster smoke as the validation gate, because the unit harness CANNOT reach the failure shape.
- The `derive_agent_url` 6-round carry is in its **8th consecutive round**. Three hardcoded `7777` sites at `restore_handler.rs:1144` (named slot variant 17777u16+vm_index), `:1662` (StubRestoreBackend literal `7777`), `:1741` (register_restored literal `7777`) — all unchanged this cycle. Canonical site `nomad_ch.rs:792 / :1532` uses `{AGENT_PORT}` constant from `zeroship-sandbox-agent::AGENT_PORT`.
- Integration test files (`crates/sandbox/tests/` + `crates/sandbox-agent/tests/`) saw **zero new tests this cycle** for the **6th consecutive cycle** (verified: `git log --oneline 1482725d..b8fae7b7 -- crates/sandbox/tests crates/sandbox-agent/tests` returns empty).

## Trend table (7-cycle test count history, updated)

| Cycle | sandbox lib | sandbox-agent lib | Δ sandbox | Δ sandbox-agent | Notes |
|-------|-------------|-------------------|-----------|-----------------|-------|
| r9    | 296         | 231               | —         | —               | baseline |
| r10   | 303         | 239               | +7        | +8              | R7-S1, R8-A2, B23, B24 +6 R-closes |
| r11   | 310         | 240               | +7        | +1              | R7-API2 + R10-Q1 + R9-S4b/c |
| r12 (start) | 319   | 240               | +9        | 0               | R11-T3 + R9-S4d + R11-P2/P3 + R11-S2 |
| r12 (HEAD)  | 327   | 242               | +8        | +2              | R12-I1 (+5) + r11 carry-finishers |
| r13 (in-flight WT) | 328 | 242        | +1        | 0               | C-3 regression test |
| r14 (HEAD `1482725d`) | 332 | 242     | +4        | 0               | C-4 retry harness (4 tests) |
| **r15 (HEAD `b8fae7b7`)** | **332** | **242** | **0** | **0** | C-7 net-zero (1 replaced) + C-6 net-zero |
| **r9 → r15 total** | **+36** | **+11**  | —         | —               | 8-round growth, **TWO cycles of stagnation now** |

The r14 → r15 delta is **0 net tests over 7 commits to `restore_handler.rs` + `admin_handlers.rs`** that touched the C-N production code path. The Δ-counts have decoupled from the bug-count: C-1...C-7 = 7 distinct bugs across 7 cluster cycles; cumulative unit-test growth attributable to C-N = 5 tests (4 c4_* + 1 c3_); cumulative integration tests added = **0** across 7 cycles. **The 0/0/0 row this cycle is the audit's first test-count flatline since r9 baseline.**

## Findings (NEW since r14)

### [R15-T1] EMERGENCY ESCALATION — 7-cycle integration-test gap now has TWO consecutive cycles where the fix landed ZERO tests (CRITICAL → EMERGENCY, test-coverage-r15, NEW elevation)

- **Files**: `crates/sandbox/src/restore_handler.rs:355` (`pub async fn restore_sandbox` — the integration entry); `crates/sandbox/src/restore_handler.rs:1018-1024` (`StubRestoreBackend::fail_reserve` USED, `fail_submit` / `fail_livez` STILL declared-but-unused for the **7th consecutive round**); `crates/sandbox/tests/sandbox_pg_e2e.rs:2707, 2757, 2785, 3237, 3359` (the 5 `restore_sandbox` callsites in pg-e2e — verified by `grep -rn "restore_sandbox(" crates/sandbox/src crates/sandbox/tests` excluding doc lines — **all 5 pass `persist=None`**, only one (`:2785`) uses `fail_reserve=true`, ZERO use `fail_submit=true` OR `fail_livez=true`); `crates/sandbox/src/admin_handlers.rs:1340-1393` (the new C-6 `teardown_thread` detach pattern — byte-coverage-zero, no test references); `crates/sandbox/src/restore_handler.rs:358-410` (`reserve_vm_index_with_retry` — the helper site of the per-attempt log + the C-7 budget — no test drops a future mid-`compio::time::sleep.await`).
- **Symptom**: smoke-r7 (`docs/reviews/sandbox-snapshot-restore-cluster-2026-05-25-T8b-smoke-r7.md`) found **C-6** (compio runtime starvation by detached teardown competing with wake handler's retry sleeps). Smoke-r8 (`docs/reviews/sandbox-snapshot-restore-cluster-2026-05-25-T8b-smoke-r8.md`) **falsified C-6's runtime-starvation hypothesis** and pinned **C-7** (ntex dropping the wake handler future mid-`compio::time::sleep.await` at client disconnect, because the 120 s retry budget exceeded the 60 s client deadline). Of these two diagnoses, the C-6 fix at `91ce9be5` landed **+0 tests** with an explicit "cluster smoke-r8 is the validation gate" disclaimer (commit body); the C-7 fix at `493d6c1e` landed **+1 / -1 tests** (NET 0) with an arithmetic-only assertion that the default policy's budget+headroom is ≤60 s. **Neither fix added any test that exercises future-drop semantics, the detached-teardown thread, OR `restore_sandbox` end-to-end past the `reserve_vm_index_with_retry` site.** Smoke-r8's diagnostic was possible only because of the C-6 phase-trace landed at `8e7f0b53` (TRACE-level wake-path logging) — i.e. observability replaced the test harness as the only diagnostic surface for cluster shapes. This is the second consecutive cycle where the C-N fix is shipped with a "cluster smoke is the validation gate" caveat, and now the audit's recommendation history is structurally degrading: r10-T1 → r11-T1 → r12-T1 → r13-T2 → r14-T1 → r15-T1 (7 rounds), CRITICAL throughout, with each cycle adding one new bug-of-the-cycle and zero new integration tests.
- **Why it matters**: r14 closed with "smoke-r6 is the empirical receipt." Cycle r15 has **two more receipts** (smoke-r7 and smoke-r8) plus a new diagnostic shape — C-7 is a future-cancellation bug class, which IS straightforwardly testable in compio (drop a `JoinHandle` mid-await, assert no row-state corruption, no slot leak), but no unit OR integration test does this. The audit's "invest in the harness" recommendation has produced **0 tests over 7 cycles** against an explicit recommendation in r10 through r14. The pattern is no longer "the unit harness lags": it is "the unit harness is structurally separated from the integration region by a 7-round-and-counting policy gap." Two additional shapes are now part of the integration gap:
  - **R15-T1a future-drop coverage**: `reserve_vm_index_with_retry` is `async` with a `compio::time::sleep(retry.interval).await` (line ~390+). The bug class is "future canceled mid-sleep → no observable failure log." NO test wraps this future in a `compio::time::timeout` or drops a `JoinHandle` for it. The C-7 unit test (`c7_retry_budget_default_is_under_client_deadline`) asserts arithmetic on the constant — it does NOT prove the loop emits an exhausted-budget log before any cancellation. A future regression bumping `max_attempts` back to 60 would compile-fail-only via `assert!(total_ms + 5_000 <= 60_000)`, but a regression that adds a SECOND inner `sleep` (or replaces `compio::time::sleep` with a non-cancel-safe primitive) would slip past silently.
  - **R15-T1b OS-thread detach coverage**: the C-6 fix at `admin_handlers.rs:1340-1393` builds a `std::thread::Builder` with name `format!("zsbx-teardown-{}", ...)`, spawns a closure that runs `compio::runtime::Runtime::new().block_on(...)`. The two failure-mode branches inside the thread closure (`Ok(rt)` → run teardown / `Err(_)` → warn "could not mint compio runtime") and the outer `spawn_res` branch (Err → warn "could not spawn detached teardown thread; vm_index will leak until orphan-prune") are 100% untested. If a future refactor changes the thread-name template, drops the `Runtime::new` line, or removes the `.unwrap_or_else` warn arms, no test fires. Commit `91ce9be5`'s self-disclosed "the bug requires a dual-task compio scenario that the current test harness doesn't drive" is now structural: NO scaffold exists in the crate that builds two ntex-worker-runtime tasks competing for poll time with a detached future.
- **Action**: **EMERGENCY ELEVATION (CRITICAL → EMERGENCY).** Six rounds was r14's stated threshold. Seven rounds, with zero net new tests this cycle, two new bugs found in cluster (C-6 + C-7), one test DELETED because it contradicted the new fix invariant, and TWO new fixes shipped with "cluster smoke is the validation gate" caveat in their commit messages, is past every threshold the audit has set. Minimum-deliverable backlog for next cycle:
  1. **One pg-e2e fixture** wiring `StubRestoreBackend { fail_submit: true, .. }` through `restore_sandbox` to drive rollback past `register_restored`. Same shape as `sandbox_pg_e2e.rs:2780-2790` (`fail_reserve=true` pattern); mirror with `fail_submit`. Asserts row reverts to `snapshotted` after rollback, vm_index released, no state-map orphan. This is the R10-T1 → R14-T1 → R15-T1 carry's primary deliverable.
  2. **One pg-e2e fixture** wiring `persist=Some(Arc<Persistence>)` through `restore_sandbox` to exercise the unseal → register_restored → clock_resync_post_restore chain. This is the R10-T2 → R14-T1 → R15-T1 sub-carry.
  3. **One unit test** wrapping `reserve_vm_index_with_retry` in `compio::time::timeout(Duration::from_millis(80), ...)` against a stub with `fail_reserve=true, interval: 50ms, max_attempts: 100` — asserts the future cancels cleanly at 80 ms with no log corruption, NO leftover reservation in `StubRestoreBackend::reserved`. This is the R15-T1a behavioural counterpart for the C-7 bug class.
  4. **One unit test** for the OS-thread detach pattern at `admin_handlers.rs:1340-1393` — extract the thread-spawn into a testable helper (`fn spawn_detached_teardown(...)`) and assert the thread name, the `Runtime::new` cycle, and the spawn-failure WARN arm. This is the R15-T1b behavioural counterpart for the C-6 bug class.
  5. **Recommend pinning #1, #3, and #4 on the next pilot cycle as a HARD prerequisite, NOT a tracking item.** Six rounds was the soft threshold; seven rounds with two more bugs is the hard threshold.

### [R15-T2] C-7 `c7_retry_budget_default_is_under_client_deadline` repeats the EXACT defect class r14-T2 flagged — constant-arithmetic-only assertion against a sibling constant (IMPORTANT, test-coverage-r15, NEW, supersedes r14-T2 closure tracking)

- **Files**: `crates/sandbox/src/restore_handler.rs:1414-1453` (`c7_retry_budget_default_is_under_client_deadline`); `crates/sandbox/src/restore_handler.rs:143-163` (`VmIndexRetryPolicy` struct + Default impl — `max_attempts: 25, interval: 2s` → 48 s observed budget vs. 55 s upper bound the test guards); commit `493d6c1e` (the test replacement — deleted `c4_default_policy_envelopes_observed_teardown` which asserted `>=90_000ms`, added `c7_retry_budget_default_is_under_client_deadline` which asserts `+5_000ms <= 60_000ms`).
- **Symptom**: r14-T2 (IMPORTANT, tracking-only) flagged that `c4_default_policy_envelopes_observed_teardown` was a constant-arithmetic assertion (`>=90_000ms`) rather than a behavioural check against the retry loop using the default policy. R14-T2 predicted brittleness to refactor — specifically the case where someone changes the constants in a way that breaks the test name's contract. The C-7 fix at `493d6c1e` is the empirical receipt: the `>=90_000ms` assertion **DIRECTLY CONTRADICTED** the new C-7 invariant (`+5s headroom <= 60s`), so the test was DELETED (commit body: "the old assertion now contradicts the new policy"). The replacement `c7_retry_budget_default_is_under_client_deadline` has the same shape — constant-arithmetic only:
  ```rust
  const NTEX_CLIENT_DEADLINE_MS: u64 = 60_000;
  const REQUIRED_HEADROOM_MS: u64 = 5_000;
  let p = VmIndexRetryPolicy::default();
  let total_ms = p.interval.as_millis() as u64 * u64::from(p.max_attempts.saturating_sub(1));
  assert!(total_ms + REQUIRED_HEADROOM_MS <= NTEX_CLIENT_DEADLINE_MS, ...);
  ```
  This is a "test-the-constant" check, not a "test-the-loop-using-the-constant" check. If `reserve_vm_index_with_retry`'s loop body is refactored to ignore `max_attempts` (e.g., add a hardcoded `min(max_attempts, 100)` ceiling, or add a SECOND inner sleep), the test passes green while production wedges past the 60 s deadline.
- **Why it matters**: r14-T2 was IMPORTANT tracking-only. The defect class it named — "test the constant, not the behaviour" — is now empirically proven to require deletion-and-replacement when the constant changes. The replacement is the SAME shape: inversion of the inequality, same arithmetic-only assertion, ZERO drive of the loop with the default policy. The 5 s headroom margin between 50 s asserted-budget and 55 s upper-bound is also closer than r14-T2's analysis of the 28 s margin (90 → 118): under the new constants, ANY single-attempt slip past 5 s of scheduler jitter on a busy worker would push the actual loop wall-time into the cancellation window — and no behavioural test would catch it. The test name promises envelope coverage; the test body delivers a constant comparator.
- **Action**: rewrite as a `#[compio::test]` that constructs `StubRestoreBackend` with a generous `reserve_succeeds_on_attempt: Some(24)` (slot frees at attempt 24 under the default 2 s cadence — ~46 s wall-time) AND uses `VmIndexRetryPolicy::default()` (not a hand-rolled fast policy). Asserts the loop succeeds within the default budget, well under the 60 s client deadline. Pair with a second test using `reserve_succeeds_on_attempt: None` + `fail_reserve: true` + `VmIndexRetryPolicy::default()` to assert the loop surfaces `VmIndexUnavailable` cleanly within ~48 s wall-time. **The actual C-7 bug class** — future cancellation mid-sleep — is testable too: wrap the call in `compio::time::timeout(Duration::from_secs(45), ...)` (5 s under the policy's 50 s budget) and assert the future cancels cleanly with no leftover state in `StubRestoreBackend::reserved`. ~60 LOC total for all three. Tracking-only this round; the C-7 fix IS correct as committed (the policy default IS within the deadline), the test just doesn't pin the property the test name claims.

## Closed by recent commits (since r14 audit at `1482725d`)

- **R11-API1 expansion** at `370fdbba` (3 orphan `#[doc(hidden)] pub fn` deletions — no test change).
- **R14-API1 (`pub(crate)` restrict)** at `00161cea` (`with_shared_allocator` + `with_nomad_handle` visibility tightening — existing tests unchanged).
- **C-6 investigation TRACE** at `8e7f0b53` (phase-tracing logs added throughout `restore_sandbox` for cluster diagnostics — no test change; the logs ARE the diagnostic).
- **C-6 fix** at `91ce9be5` (OS-thread detach pattern for `admin_handlers.rs:1340-1393` — **+0 tests**, explicit "cluster smoke-r8 is the validation gate" in commit body).
- **C-6 closure doc** at `bd2d4a20` (doc-only).
- **R14-Q3 + R14-P2** at `9afd0986` (`snap-l2-upload` thread-name builder simplification — no test change).
- **R14-Q2 (`seal_filename_for_str` cfg-gate)** at `79b4d258` (build-only).
- **Pilot artifact bundle** at `7dc7565e` (r14-round-2 reviewer artifacts + closures — doc-only).
- **Controller pin bumps** at `79101a99` (v21 → v22) and `6c6a6d3a` (v22 → v23) (script-only — no Rust test).
- **Smoke-r7 review** at `d673e043` (doc-only — C-6 LOCALIZED).
- **Smoke-r8 review** at `baaa8d74` (doc-only — C-6 falsified + C-7 NEW).
- **C-7 fix** at `493d6c1e` (`VmIndexRetryPolicy::default` 60×2s → 25×2s + per-attempt INFO log; **+1 test `c7_*` / -1 test `c4_*`**; net 0).
- **C-7 closure doc** at `b8fae7b7` (doc-only).

### NOT closed by r15 cycle (the central question, restated)

- **[R10-T1 / R11-T1 / R12-T1 / R13-T2 / R14-T1 → R15-T1]** restore_sandbox end-to-end rollback + Persist(Some) chain — **7th-round critical, ELEVATED to EMERGENCY this round.** No pg-e2e fixture wires `fail_submit=true` OR `fail_livez=true`; `Persist(Some(_))` chain at `restore_handler.rs` post-livez (unseal + register_restored + clock_resync_post_restore) STILL has no pg-e2e driver.
- **[R13-T1]** C-3 regression test fidelity concern — **NOT addressed.** Test body at `snapshot_store_gcs.rs` unchanged from r13. 3rd-round carry.
- **[R13-T3 / R11-A1 helper extract]** R9-S4 family error-envelope divergence (5 sites, 3 `String` + 2 `DatabaseError::Validation(msg)`) — **NOT addressed.** Only persist.rs touched since r14 (R14-Q2 cfg-gate, +6 LOC doc/cfg). 3rd-round carry. Consolidation cost-per-cycle continues monotonic upward drift.
- **[R14-T2 → R15-T2]** `c4_default_policy_envelopes_observed_teardown` (the SUBJECT of r14-T2) was DELETED by the C-7 fix because it contradicted the new invariant. **The replacement `c7_retry_budget_default_is_under_client_deadline` repeats the exact same defect class** (constant-arithmetic-only). 2nd-round carry under new name; the deletion-and-replacement IS the r14-T2 prediction's empirical receipt.

## Carry-forward (still open from earlier rounds)

- **[R10-T1 / R11-T1 / R12-T1 / R13-T2 / R14-T1 / R15-T1 (re-filed, 7th round, EMERGENCY)]** `restore_sandbox` end-to-end rollback through R10-C1 + R10-C2 fixes — still uncovered. **Seven-round emergency.** Cross-lens consensus (architecture r13 R13-A1 + test-cov r10 → r15 all CRITICAL/EMERGENCY). Smoke-r7 + smoke-r8 are the cycle's empirical receipts.
- **[R10-T2 / R11-T2 / R12-T2 / R13-T2 / R14-T1 / R15-T1 (re-filed, 7th round, EMERGENCY)]** `Persist(Some(_))` chain at `restore_handler.rs` — still uncovered. **Seven-round emergency.** Same root cause as the row above; same fix lands both.
- **[R15-T1a (NEW sub-finding)]** future-drop / cancellation coverage of `reserve_vm_index_with_retry`'s `compio::time::sleep.await` site — ZERO tests drop the future mid-sleep to pin the C-7 bug class. Recommended pin: `compio::time::timeout(d, reserve_vm_index_with_retry(..))` with `fail_reserve: true` + assert clean cancellation + no orphan reservation.
- **[R15-T1b (NEW sub-finding)]** OS-thread detach pattern at `admin_handlers.rs:1340-1393` — byte-coverage-zero. Commit `91ce9be5` self-disclosed "the bug requires a dual-task compio scenario that the current test harness doesn't drive." No scaffold exists. Recommended pin: extract to `fn spawn_detached_teardown(...)` + unit test the thread-name, Runtime::new cycle, and spawn-failure WARN arm.
- **[R9-T6 / R11-T4 / R12-T4 / R13 carry / R14 carry (re-filed, 8th round)]** `derive_agent_url` parity — drift still THREE hardcoded `7777` sites. Re-verified at HEAD `b8fae7b7`: `restore_handler.rs:1144` (`format!("http://127.0.0.1:{}", 17777u16 + ...)` — StubRestoreBackend variant), `restore_handler.rs:1662` (`format!("http://10.{}.{}.2:7777", ...)` — RealRestoreBackend `derive_agent_url`), `restore_handler.rs:1741` (`format!("http://10.{}.{}.2:7777", ...)` — `register_restored` agent_url). Canonical site `nomad_ch.rs:792 / :1532` uses `{AGENT_PORT}` from `zeroship-sandbox-agent::AGENT_PORT`. r15 cycle did not touch any of these. **Eighth-round carry.**
- **[R10-T3]** `read_sandbox_id_from_sources` cross-source (env-empty + file-present) test in `crates/sandbox-agent/src/handlers.rs`: still 7 tests, none with both env-AND-file set. (6th-round carry.)
- **[R10-T4]** `RootKek::from_env` (the production entry called from `AppState::from_config`) under uid invariant: still only `from_path`-driven tests. (6th-round carry.)
- **[R10-T5]** `TieredSnapshotStore::put` L2-detach behavioural test: r13's C-3 test at `snapshot_store_gcs.rs:1405-1439` partially exercises spawn-side; NEITHER fire-and-forget joinability NOR L2-failure / L1-success-divergence is pinned. (6th-round carry.)
- **[R9-T3]** `do_restore_inner` spawn_blocking panic-recovery branches — STILL EIGHT sites with the JoinHandle discarded via `let _ = ... .await`. (7th-round carry.)
- **[R9-T5]** `build_restore_nomad_job_json` integration via `submit_restore_job` STILL untested. r12-I1's +5 builder-shape tests STILL don't drive the env→mode→jobspec→POST chain. (6th-round carry.)
- **[R9-T8]** Sweep takeover end-to-end for `Restoring` / `RestoringCold` transient states — only `Snapshotting` exercised at the pg-e2e level. (7th-round carry.)
- **[R9-T9]** `/_clock_resync` 4 KiB body-cap 413 test — still no analog of `proxy_http_body_cap_413`. (7th-round carry.)
- **[R9-T10]** `clock_resync_post_restore` controller-side slow-agent + 500-from-agent + malformed-body tests: still 4 tests only. (7th-round carry.)
- **[r4-T2 / r5-P1b / r7-P1 / r8 / r9 / r10 / r11 / r12 / r13 / r14 wake-path latency canary]** still no compio-tick canary in `do_snapshot_inner` or `do_restore_inner`. (11th-round carry.)
- **[r3-T3 / r5-T2 / r6 / r7 / r8 / r9 / r10 / r11 / r12 / r13 / r14 wrapper script behavioural coverage]** still ZERO behavioural test for `crates/sandbox/scripts/nomad-vm-wrapper.sh`. (12th-round carry.)
- **[T8 ControllerIdleSnapshotter non-pg coverage]** still byte-coverage-zero outside cluster smoke at `crates/sandbox/src/sweep.rs:314-407`. (6th-round carry.)
- **[r6 MEDIUM script artifact consistency tests]** still unwritten. (9th-round carry.)
- **[r8 LOW sig.rs:1475 1-day-future skew-bypass]** still couples nonce-replay assertion to `ts_now() + 86_400`. (7th-round carry.)
- **[R9-P2 (deferred) A2b `verify_metadata_only` zero callers]** still untested at `snapshot_store_gcs.rs`. (7th-round carry.)
- **[R11-T5 / R13-T3 (re-filed, 4th round)]** `read_root_owned_secret_file` extract still blocked on R13-Q2 error-envelope harmonisation. 8+ near-identical sibling tests across 5 sites unchanged this cycle.
- **[Integration test dir stasis]** `crates/sandbox/tests/` + `crates/sandbox-agent/tests/` — **zero new tests for 6 consecutive cycles**. Verified at HEAD `b8fae7b7`: `git log --oneline 1482725d..b8fae7b7 -- crates/sandbox/tests crates/sandbox-agent/tests` returns empty.

## Notes for the next round

**The central question this cycle (per brief): after 9 cluster cycles and 7 bugs, has anything closed the structural test-coverage gap?** **Answer: NO. The gap widened.** Cycle r15 is the first cycle since r9 baseline with **zero net new tests** in either sandbox crate. The C-6 fix at `91ce9be5` landed +0 tests with explicit "cluster smoke is the validation gate" caveat. The C-7 fix at `493d6c1e` landed +1 / -1 = 0 net tests AND deleted the r14-T2 IMPORTANT subject (the `c4_default_policy_envelopes_observed_teardown`) because it contradicted the new invariant — exactly the brittleness r14-T2 predicted. **The replacement c7_* test has the SAME defect class** — constant-arithmetic-only assertion. r14-T2 is empirically receipted AND immediately re-filed as r15-T2.

**Smoke-r7 + smoke-r8 are the cluster's empirical confirmation that observability has replaced testing as the primary diagnostic surface.** The C-6 phase-trace at `8e7f0b53` (INFO-level wake-path logging) was the ONLY tool that could localize the C-6 wedge, AND the C-6 hypothesis it produced was then falsified by smoke-r8's per-attempt log delta — i.e. the unit test harness contributed zero signal in two consecutive bug-discovery cycles. The audit's 7-round recommendation chain "invest in the harness" has produced ZERO integration tests; meanwhile observability investment has produced two cluster-actionable diagnostics in two cycles. This is a structurally backward trade for any team that expects test-driven regression coverage.

**R15-T1 ELEVATION RATIONALE (CRITICAL → EMERGENCY):**
1. r14 closed with "six rounds is the cycle-pressure threshold at which the audit must either escalate or formally close the finding as ACCEPTED-RISK." Seven rounds, with zero net new tests, is past that threshold by exactly one cycle.
2. Two new bugs (C-6, C-7) were found in cluster between r14 and r15. The bug rate is sustained 1-per-cycle. The test rate is now 0-per-cycle. The integration-coverage delta is monotonically negative.
3. The r14-T2 prediction (constant-only assertions are brittle to refactors of the constant) was DIRECTLY VINDICATED by the C-7 fix deleting `c4_default_policy_envelopes_observed_teardown`. The audit's IMPORTANT-tier findings now have a one-cycle predictive track record; ignoring the CRITICAL-tier carry is no longer a defensible policy.
4. TWO consecutive cycles (C-6 + C-7) had commit messages that explicitly state "cluster smoke is the validation gate, the test harness cannot reach this shape." This is no longer an audit observation — it is a fact admitted in the fix commits themselves.

**Recommended pilot directive for r16**: pin R15-T1's #1 + #3 + #4 (pg-e2e `fail_submit`, future-drop unit test, OS-thread detach unit test) as HARD prerequisites — not tracking items. Until at least one lands, the audit's recommendation cascade is degrading: each new cycle adds one new bug + one new carry + zero new tests, and the recommendation chain's signal-to-noise approaches zero.

**R15-T2 priority**: rewrite as a `#[compio::test]` driving the default policy through `reserve_vm_index_with_retry` (not asserting on the constants alone). Lower priority than R15-T1 (the policy IS correct as committed), but the test name's promised contract is structurally undelivered. The next time `max_attempts` or `interval` changes, this test will get deleted-and-replaced AGAIN, repeating the C-7 ↔ C-4 deletion cycle. The audit predicts: WITHOUT a behavioural rewrite, the next time the C-7 budget is refactored (when C-7-LT lands async-poll, or when a future cluster cycle tightens the deadline), `c7_retry_budget_default_is_under_client_deadline` will be deleted in the same way `c4_default_policy_envelopes_observed_teardown` was deleted this cycle. Same defect class, same end-state.

**Cycle defining pattern (r15 update)**: r13 noted "firefighting beats systematic drain." r14 noted "firefighting now provably misses the fire." r15 notes "firefighting has stopped adding tests entirely — the harness is in active regression while observability picks up the diagnostic load." The trajectory is: r10–r12 added 3–9 tests per cycle (healthy growth); r13–r14 added 1–4 tests per cycle (slowing); r15 added 0 net tests (flatline). Smoke-r7 + smoke-r8 are the second and third consecutive "1-cycle-1-bug" results. Until ONE integration fixture lands wiring stub failure flags through `restore_sandbox`, every cluster smoke cycle is a guarantee of finding C-(N+1) on the next retry — AND a guarantee that the next C-N's fix will be net-zero tests AND will likely require deleting an existing test whose constants contradict the new invariant.
