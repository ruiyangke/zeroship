{
  "name": "s3-js-compare",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Official @aws-sdk/lib-storage multipart-upload oracle — compares correctness + throughput against zeroship's compio-s3 env.storage multipart path (ISS-32). Standalone (NOT a workspace member); npm install here.",
  "scripts": {
    "upload": "node upload.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/lib-storage": "^3.700.0"
  }
}
