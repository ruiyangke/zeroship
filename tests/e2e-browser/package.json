{
  "name": "@zeroship/e2e-browser",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Browser-tier end-to-end suites: one per demo under examples/, driven by vitest + Playwright against each demo's real dev server.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "playwright": "1.58.2",
    "typescript": "catalog:",
    "vitest": "^4.1.7"
  }
}
