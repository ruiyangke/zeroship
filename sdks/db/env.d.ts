// Retired: `@zeroship/db/env` no longer derives `Env.db` from a declared
// `zeroship-schema` alias. Apps get strong `env.db` typing from the generated
// `generated/zeroship/env.db.ts` file emitted by the Vite plugin's in-process gen-types path.
export {};
